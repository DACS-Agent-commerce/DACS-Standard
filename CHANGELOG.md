# DACS Changelog

All notable changes to the Demos Agent Commerce Standards.

This document follows the spirit of [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). The format is adapted for a standards document rather than a software project: the focus is on normative changes that affect implementers, not on internal editorial revisions.

The format used per release:
- **Added** — new normative requirements, new fields, new sections.
- **Changed** — modified normative requirements; semantics changes; renames.
- **Removed** — retired normative requirements; deleted fields; obsolete framing.
- **Fixed** — spec defects (rules that referenced undefined values, internal inconsistencies, addressing patterns that did not work on the named substrate).
- **Governance** — changes to stewardship, working-group framing, or progressive-anchoring phase.

## [Unreleased]

### Fixed — Demos SR-2 bundle co-signing status

- **Artifact signatures separated from transaction signatures** (#369) —
  corrects DEMOS-MAPPING §A.2 so a native multi-party Storage Program
  transaction is not presented as an amber DACS v0.1 dependency. For non-abort
  outcomes the bundle first carries every required DACS signature and each
  required signing party anchors its role-specific copy through its own
  owner-signed SR-2 write and verified receipt. Abort outcomes retain the
  single-signature and bundle-suppression exceptions in §§10.4.1/10.11. Native
  transaction co-signing remains an optional optimization and cannot replace
  applicable artifact signatures or role-specific publication.

### Fixed — lifecycle walkthrough current-wire compatibility

- The executable lifecycle walkthrough now emits a canonical ULID `jobId`,
  omits the non-schema `phaseIndex` from signed `SettlementEvidence`, recovers
  the payment phase tuple from its canonical PC-2 logical address, authenticates
  that tuple against the published logical/native/content binding and reference,
  and emits failure evidence with `outcome: "failure"` and a non-empty `reason`
  (#357). The signed bundle phase summary remains the delivery-phase binding.
  The walkthrough's pinned DACS-4 v0.3 profile intentionally keeps the legacy
  `evm` reference and does not advertise the v0.6+ SB-1 event-identity rule.

### Added — DACS-4 v0.6 pay-ap2 hardening

- **(AP2-6)** pins the provider idempotency-key derivation byte-for-byte
  (`dacs-ap2-idem:v1:` + NFC(jobId) + `0x3a` + minimal decimal phaseIndex),
  registers that preimage tag as a non-signature hash domain, and requires an
  incapable provider to be refused at rail registration (#276).
- **(AP2-7)** binds each AP2 `transaction_id` to one `(jobId, phaseIndex)`.
  An exact-tuple retry resumes with the same AP2-6 key and can never create or
  count a second payment; cross-job/cross-phase reuse rejects, and conflicting
  stored bindings fail closed.
- **Executable AP2 handler-safety candidates** cover idempotency-key bytes and
  NFC; exact compact-JWS `transaction_id` derivation with `_sd_alg` selection;
  separate CheckoutMandate + PaymentMandate admission before AP2-7/provider
  side effects; exact-tuple retry/resume; cross-session/cross-phase replay;
  the DACS checkout-signature profile; and split-credential registration.

### Changed — DACS-4 v0.6 pay-ap2 composition

- **(AP2-2)** keeps the universal provider-receipt rule bounded by the selected
  SR-3 binding's authenticated property, while scoping consensus-anchored
  fetch-and-hash (not validator-body-signing) to current Demos DAHR (#280).
- **(AP2-3)** names the distinct privileged create/metadata credential and
  relayed read-only status credential, and makes ability to provision the
  latter a registration-time requirement (#282).
- **Step 2** requires the handler to possess and verify separate CheckoutMandate
  and PaymentMandate artifacts, derives `transaction_id` from the exact compact
  checkout JWS using CheckoutMandate `_sd_alg` with SHA-256 fallback, and rejects
  malformed, unsupported, or mismatched derivations before AP2-7 reservation or
  provider submission (#284). DACS explicitly profiles the stricter branch of
  AP2 v0.2's contradictory checkout-signature guidance and requires
  non-deterministic signature generation (#281); DACS artifact algorithms are
  unchanged.
- **`docs/flow-trace.md`** recognizes the AP2-3 read-only status fetch as the
  narrow credential-bound DAHR carve-out (#279).

### Fixed — DACS-1 v0.7 / DACS-2 v0.6

- **Presence-only claim requirements made executable** (DACS-1 §6.3.3
  PCR-1..PCR-6; DACS-2 §7.7.1; #334) — resolves the contradiction where
  `verificationRequired: false` skipped the decision check but still failed the
  unconditional freshness gate, while composite aggregation required a passing
  `VerifyResult` for every member. Presence-only matching now evaluates the
  exact signed bundle directly: canonical known claim, unexpired presenter
  bound, and presenter-signed scheme parameters, with `issuedAt` informational. A well-shaped
  optional `verifiedBy` is non-deciding for presence; malformed references are
  errors, and `maxAge`/`recipeVersion` are invalid without verification.
  DACS-2 excludes presence-only members from result evidence and replays mixed
  required/`oneOf` aggregation against the exact bundle and requirement bound
  by the existing CVR hashes; missing bundle is indeterminate, while synthetic
  results, substitutions, invalid signatures, and decision mismatches reject.
  Presence remains neither verification nor control: the exact `key:` bundle
  signature may independently prove key control, but existence-only `lei:`
  presence cannot become `presentedBy`, elevate identity tier, or key
  reputation. Aggregation always authenticates the session-pinned recipe
  registry snapshot, including for an all-presence requirement, so the
  algorithm remains consistent with CRQ-1 and composes with descriptor-bound
  registry resolution. Empty collection and exact-boolean configuration
  semantics are explicit. Adds 38 deterministic vectors with genuine Ed25519
  bundle, VerifyResult, and composite signatures. No artifact or schema change.

### Added — signed alternative-payment projection

- **One Listing, one buyer-selected payment handler** (DACS-1 §6.3.4,
  DACS-3 §8.5.2, DACS-4 §9.9.1 APR-1..APR-8, DACS-5 §10.4.3; #340) — adds
  the listing-only `pay-alternative` phase carrying complete
  `PaymentRailRef` alternatives. Exactly one reference is selected into the
  signed Agreement before commitment, its authenticated concrete handler is
  projected at the original phase index, and evidence/bundles record that
  concrete handler. Post-signature switching requires a fresh job and retries
  never fall through to another rail after authorization or ambiguous
  settlement. A claimed fresh-job replacement is now signed through
  `priorPaymentDispositionRef` and can authorize only from an authenticated,
  finalized `PriorPaymentDisposition` that durably closed before authorization
  or proves the prior authorization cannot settle. Legacy readers reject the
  unknown phase; ordinary and repeated payment pipelines retain their prior
  meaning.

### Fixed — DACS-1 / DACS-4 rail availability

- **Production rail selection and authoritative hints** (DACS-1 §6.3.4
  LRR-6; DACS-4 §9.4.4 RAV-R1..RAV-R5; #325) — makes the existing mocked-rail
  prohibition executable: no new session can select `disabled` or `failed`,
  and a new production session also cannot select `mocked`. Preserves the
  documented distinction that an already-pinned session may continue after a
  later `disabled` revision,
  without permitting a new or replacement session. Discovery and catalog
  availability values are explicitly non-authoritative prefilters or UI
  hints and cannot establish, refute, or override the signed pinned result.
  Production/non-production mode is trusted local operator policy, never a
  counterparty or protocol-artifact input. Replaces the non-existent runner
  claim with a deterministic Ed25519 vector generator and executable CI
  evaluator covering 28 cases over complete signed `RailDefinition` documents,
  including mocked, disabled new/in-flight behavior, stale and forged
  definitions, missing authority, signed-scope mutation, untrusted mode input,
  and contradictory discovery hints.

### Added — DACS-4 v0.6

- **Signed settlement-event identity — SB-1/SB-2 repair** (§9.3, §9.5.2, §9.5.3, §9.5.7, §9.5.8; #315) — adds distinct `evm-event`, `solana-instruction`, and `x402-event` `ChainTxRef` arms so the event/instruction coordinate that produces the SB-1 uniqueness key is inside the signed `SettlementEvidence` scope. Current producers must emit the applicable event-level arm and verifiers independently match its asset, payer, agreement-authorized payee, signed `paymentAmount`, and receipt context against authenticated ledger data before projection. They must also compare the complete PC-2 address tuple against signed `jobId`, the authenticated agreement/phase rail, and the authenticated `BundlePhaseEntry.index`; a valid evidence signature or outer receipt cannot substitute for this check. The legacy `evm`, `solana`, and `x402` arms remain byte-stable read/replay shapes: exactly one independently matching event permits projection, no match fails, and unavailable or multiple matches remain `indeterminate`; unsigned caller/indexer coordinates never disambiguate them. Adds deterministic, genuinely signed vectors for EVM, Solana, current and legacy x402 receipt/event reconciliation, batched transfers, cross-job reuse, signed-amount mismatch, full-address tuple mismatch and CF-4 encoding, malformed/missing indexes, ledger mismatch/unavailability, legacy ambiguity, discriminator stripping, and cross-type signature replay.

### Fixed — DACS-2 v0.5

- **Method-conditional recipe parsing** (DACS-2 §7.4.1 PRA-1..PRA-5 and
  §7.6; #318) — makes `Recipe.parserRules` optional at the wire level but
  normatively required whenever a recipe declares a parser-consuming method.
  Native-only recipe authors must omit it and the steward must reject new
  submissions that carry it, while readers ignore any such member regardless
  of value so they need no unverifiable historical-version operand. Mixed
  recipes carry one ParserSpec shared unchanged by every parser-consuming
  member; authors and stewards reject combinations whose authenticated outputs
  require different parsers, which instead use distinct recipe families. A
  method-native selection skips it completely. Missing or invalid required
  parsers and unknown methods fail before invocation or external side effects.
  `VerifyResult.data` may be sourced from authenticated method-native output or
  ParserSpec extraction.
  Adds executable vectors covering all nine registered methods, inert native
  parser compatibility (including `null` and a parser result that contradicts
  the native result), invalid required presence, mixed-method selection, a
  three-parser-method shared-rule invariant, and unknown selection.

### Added — DACS-1 v0.6 / DACS-2 v0.4

- **Canonical domain identity and persistent Demos GCR verification**
  (DACS-1 §6.3.1 DCR-1..DCR-8; DACS-2 §7.3.10 DGCR-1..DGCR-6; §A.1;
  #275) — makes `domain:<lowercase-IDNA-hostname>` the only producer form and
  keeps historical `web2:domain:<host>` as a permanent read/replay alias.
  Readers verify original signed bytes before semantic normalization and
  collapse both aliases before matching, tier derivation, `oneOf`, and
  reputation. Registers the separate `demos-gcr-domain` recipe family for
  authenticated, finalized `web2.domain` GCR records: exact host/account,
  proof URL, source transaction, and inclusion time are checked; the
  consensus transition authenticates registration-time Ed25519 verification
  of `dacs-domain:v1:<host>:<account>` without re-fetching the proof; unavailable GCR
  authority remains `indeterminate`; and inclusion time, not query time,
  governs the effective window. The persistent record cannot satisfy a fresh
  `domain-tls-control` requirement and controls a domain only when the bundle
  presentation verifies under the same GCR-bound account. Carries 63
  deterministic Ed25519 vectors (61 valid and two deliberately corrupted)
  and changes the example producer output to
  canonical `domain:`. The steward's signed registry publication remains a
  separate post-review operation.

- **DCR/DGCR conformance completion and spelling boundary** (DACS-1 §6.3.1
  DCR-1/DCR-2/DCR-4/DCR-5/DCR-7; DACS-2 §7.3.10
  DGCR-1/DGCR-2/DGCR-4; #332) — closes the remaining producer/read ambiguity:
  a current U-label or upper-case `domain:` spelling is rejected rather than
  repaired, while an otherwise-valid historical `web2:domain:` hostname is
  ASCII-case-folded only after its original signature verifies. Expands
  `domain-claim-gcr-v0.4` with those executable distinctions plus length and
  A-label boundaries, non-IP numeric-host and distinct-host cases, direct/node
  writer authorization, finalized inclusion, presentation-bound SR-1 control,
  native context, and inclusion-time anti-reissue coverage. The generator is
  checked byte-for-byte in CI.

- **Signed bundle-version and verify-before-fold boundary** (DACS-1
  §6.3.1 DCR-1/DCR-4/DCR-5; #347) — removes the corpus-only
  `producerDacs1Version == "0.6"` selector from signed bundle bytes. Exact
  spelling now follows the `domain:` scheme without a profile sidecar. Because
  frozen `IdentityBundle.bundleVersion: "1"` has no authenticated minor-version
  discriminator, current alias emission is tested at the producer/serializer
  boundary from structured `producerInput` while readers permanently accept
  signature-valid legacy aliases; adapters without that producer boundary must
  abstain rather than replay the reader fixture. Mutable deployment state cannot
  reclassify retained bytes. Adds single- and dual-alias current-production
  rejections, `presentedBy` coverage, and paired signed/signature-corrupted
  malformed-host rows that make verify-before-fold ordering evaluator-falsifiable.

### Added — DACS-4 v0.5

- **Payload-bound attested delivery — `PayloadAttestationRecord` and DPA-1..DPA-9** (§9.6.3, §9.7, §B.1/§B.3/§B.7, §A.3; #300) — closes the path where `deliver-attested-payload` could be counted from seller/orchestrator-signed `SettlementEvidence` without the declared verification method or a payload-bound proof. A distinct minor-safe artifact (`payloadAttestationVersion: "1"`, `dacs-payload-attestation:v1:`) binds the exact delivered cleartext digest to `jobId`, the committed agreement, the signed DeliverableSpec, and the selected verification method; its method evidence remains independently resolved and verified. Listings selecting the phase must declare a usable method before any payment, success evidence must carry content hash + anchor + a ref to a passing payload record, non-pass/unresolved outcomes never collapse to success, and ordinary evidence signatures cannot substitute for method proof. The Demos binding carries DAHR's `responseHash`/`responseHeadersHash`/`txHash` through the method-evidence chain, requires an authenticated resolvable `web2Request` transaction at `included` or stronger (with finalization before terminal DACS-5 production), and limits the current string-returning API to byte-exact UTF-8 payloads. The legacy optional wire spelling of `verificationMethod` is unchanged, but DPA-1 deliberately changes behavior and reject timing: a missing method now rejects before session start and payment instead of potentially failing only when delivery is attempted. Adds candidate vectors covering binding, replay, type/domain separation, DAHR transaction/hash requirements, self-signed disclosure, and fail-closed resolution.

### Added — DACS-5 v0.4

- **DACS-5 v0.4 evidence-bound fault bundle — `EvidenceBoundFaultAttestationBundle`, SEB-1..SEB-6** (§10.4.1, §10.4.3, §14.5, CORE §B.7) — adds a distinct signed bundle type whose authoritative top-level `settlementEvidence[]` is a duplicate-free bijection with the DACS-4 payment/delivery results authenticated by the signed listing and signed bundle `phaseSummary`. Consumers derive repeated, failed, aborted-before-result, and ST-8 membership deterministically; preserve a validated SEB result across EBFAB/EBFAB, EBFAB/FAB, and EBFAB/legacy reconciliation; and reject missing, extra, duplicate, aliased, reused, dangling, misbound, or inadmissible members. The exact SR-2 gate is `included` or `finalized` for failed/aborted evidence, and `finalized` plus independent resolution for every successful completed-bundle dependency under ST-11. The new discriminator, bundle and extended-pointer signature domains, and signed compatibility fixture preserve the released bundle types and reject unknown/stripped discriminators and cross-type signature replay. Candidate vectors remain non-golden; formal run-file convergence under `CROSS-RUN.md`, a Demos cross-run, and golden promotion remain pending.
### Fixed — DACS-1 / DACS-4 chain applicability

- **Byte-exact `cci-xm` → EVM rail-chain predicate** (DACS-1 §6.3.1;
  DACS-4 §9.4.3 RD-5 / §9.5.1 PB-2; #307) — defines the PB-2 EVM claim
  profile as `cci-xm:evm:<eip155-chainId>:<address>` and compares its CAIP-2
  `eip155:<chainId>` value byte-for-byte with the pinned rail definition's
  EVM network. Generic labels such as `mainnet`, `testnet`, and `sepolia`
  remain readable but are never guessed into a security-bearing chain match;
  this revision's legacy-alias table is empty. A numeric mismatch or
  non-profile label leaves tier 2 inapplicable and tier 3 legal. The family is
  the lowercase literal `evm`; the address component is required to be
  non-empty but is otherwise opaque to chain selection, and ClaimReference
  parameters do not change the derived chain. An exact match makes tier 2
  applicable before SR-1 resolution and therefore preserves the existing
  no-downgrade pause/error behavior. RD-5 now rejects conflicting EVM
  asset/network chain IDs. Adds 20 executable candidate vectors. Bumps
   **DACS-1 and DACS-4 to v0.5** without changing an artifact shape.

### Fixed — DACS-2 v0.3

- **Complete `ClaimRequirement` qualification — CRQ-1..CRQ-4** (§7.4.3, §7.6.1, §7.7.1, §7.12, §14.2) — filters authenticated resolved `VerifyResult` candidates by the effective exact recipe-family version—an explicit requirement pin or that exact family's latest version in the authenticated session-start registry snapshot—and governing freshness plus any additional listing-declared age bound, then requires the declared parameter-key subset for a passing result before required-claim or `oneOf` decision classification. An explicit or implicit version that cannot be resolved is `error` before classification; a non-live implicit latest is also `error` and never falls back to an older live version. Cache eligibility is separated from aggregation applicability: a cross-session pass is fully requalified, while a non-pass requires authenticated exact originating-parameter equivalence or a current-predicate rerun. Same-scheme results can no longer cross-satisfy requirements with different constraints; extra unrequested result data remains valid; applicable current-session `error`/`indeterminate` decisions and existing precedence remain unchanged. Production uses the orchestrator-owned active `SessionContext` and requires the separate `VetCredentialsInput.recipeRegistryVersion` to equal that context's pin before resolution. This binds the exact requirement bytes evaluated but does not claim cross-party authorship/provenance for a complementary requirement. Replay derives the pin only from a cryptographically verified signed `AttestationBundle` or `FaultAttestationBundle` that matches the record job and contains its exact validated reference; the referenced composite record, its `requirementHash`, and participating `VerifyResult` artifacts are hash- and signature-verified, while the off-chain unsigned `SessionRecord` is explicitly refused as authority. Adds thirty-six candidate security vectors, including exact family/version availability, reuse/rerun, genuine signed-bundle replay, caller-session and tampered-signature substitution, production pin mismatch, wrong-job bundle substitution, missing record-reference, requirement and result-projection substitution, and unsigned-session-record controls, plus executable semantic, signature, and guard-mutation checks. `CompositeVerificationRecord` and `VerifyResult` v1 remain unchanged. Bumps DACS-2 to v0.3.

### Added — DACS-5 v0.4

- **Settlement-verified reputation derivations — RSV-1..RSV-4** (§10.5, §14.5) — adds `SettlementVerifiedReputationDerivation` and `ReplayableSettlementVerifiedReputationDerivation` with exclusive structural discriminators, preserving the released DACS-5 v0.3 `derivationVersion: "1"` and `replayableDerivationVersion: "1"` semantics. Settlement-verified derivation compares the full canonical cross-copy `settlementEvidence[]` reference multiset, requires every presented reference to pass DACS-4 content/signature and independent Agreement/session/phase/rail/transaction/finality verification, excludes rejected or indeterminate jobs without inventing fault, and admits volume only with verified successful DACS-4 payment evidence. Unsupported readers reject the new type before type-specific action; no repository-revision knowledge is needed to distinguish pre-RSV from RSV-enforced output. Adds six v0.4 reference-divergence vectors and seventeen v0.4 semantic-admission vectors; completeness and optional per-phase `attestationRef` remain separate.

### Fixed — conformance

- **Canonical JSON cross-implementation repair** (CORE §B.2; #270) —
  clarifies that the DACS 2^53−1 constraint is a finite-number magnitude
  profile, not an integer-only profile, and makes `scripts/jcs.py` serialize
  bounded fractional binary64 values using RFC 8785's ECMAScript form instead
  of rejecting them. CF-1 now explicitly normalizes string values only:
  member names remain as received, sort by UTF-16 code units, and NFC/NFD
  spellings remain distinct. Adds 25 byte-discriminating candidate vectors
  covering the seven observed cross-run divergences and relevant RFC edge
  cases, including signed minimum subnormal, integral-binary64 formatting, and
  both binary64 safe-magnitude boundaries. Each adapter must compare the exact
  canonical bytes before emitting its verdict because the generic run-file
  channel itself is verdict-only. Cross-run tooling records unsupported cases
  as abstentions, excludes them from agreement denominators, refuses to label
  a one-impl or abstaining run converged, and requires rule-by-rule
  discrimination evidence before candidate promotion.
- **One-sided bundle hash regenerated** (DACS-5 §10.4.1 / §14; #327) —
  replaces the stale `verify-consume-one-sided` manifest hash after the
  normative reference-shape migration, binds manifest regeneration to the
  regenerated signed fixture, and refreshes the lifecycle manifest/trace
  pins. Adds a regression test that verifies both the exact bundle hash and
  its deterministic Ed25519 signature. No normative protocol rule changes.
- **Preserve-unknown Listing fixture aligned with DPA-1** (CORE SIG-5;
  DACS-4 §9.6.3 DPA-1; #323) — adds an explicit supported `self-signed`
  verification method to both signed Listings, then deterministically
  regenerates their artifact hashes and Ed25519 signatures. The executable
  suite now evaluates signature validity, phase support, and DPA-1 eligibility
  before comparing each vector's declared overall Listing disposition. No
  normative protocol rule changes.
- **Lifecycle-vector content hash corrected to the §B.2 canonical form** (CORE
  §B.2 / §B.7, DACS-5 §10.4.1; #278) — `scripts/validate_conformance_vectors.py`
  hashed each artifact whole, signature included, via `json.dumps`, so no
  published vector exercised the §B.2 canonical-form hash of a *signed* artifact
  (an implementer could be §B.2-correct yet fail, or §B.2-wrong yet pass). The
  validator now canonicalises with a vendored stdlib-only module (`scripts/jcs.py`)
  implementing RFC 8785 (JCS) for the DACS finite-number magnitude profile,
  strings, literals, arrays, and objects (fractional serialization completed by
  the #270 repair above). It hashes the signature-omitted scope via an explicit per-kind
  hash-excluded-field table (`signature` / `signatures`; bundles also omit
  `anchoredByRole` per §10.4.1). It additionally verifies every embedded ed25519
  signature over the §B.7 domain-separated payload (registry-validated separator ‖
  lowercase-hex artifact hash) and pins each result two-way in a new per-artifact
  `signatureChecks` field, so `neg-bundle-tampered-signature` stays discriminated
  even though its tamper lives entirely in the §B.2-omitted field (under the
  corrected hash it is byte-identical to the happy bundle). The 10 envelope
  `contentHash` values were regenerated with `--write`; the embedded signatures
  already committed to the §B.2 hash, so no re-signing was needed. The
  `json.dumps` → JCS switch changes no byte on this all-ASCII, float-free corpus
  (pinned by an executed per-artifact equivalence test). Per CF-1's literal "every
  JSON string value", NFC is applied to string values only; member names are
  serialised and UTF-16-sorted as received (matching the in-repo `nfc_deep`
  precedent and now explicit in CORE §B.2 via #270).
  Residual: the signed internal cross-references
  (`listingRef` / `vetRecordRef` `contentHash`) still commit to the legacy
  whole-artifact hashes and cannot be corrected in place without the external
  signing keys — tracked in #313; these fixtures MUST NOT be used as
  reference-resolution vectors until regenerated generator-side. No normative
  protocol rule changes.
- **Artifact reference oracle regenerated** (DACS-2 §7.5.2, DACS-4 §9.3,
  DACS-5 §10.4.1; #308) — replaces legacy `{kind,id,contentHash}`
  `AttestationRef` objects in every shared bundle fixture position with
  `{anchor:{kind,locator},contentHash,signer?}`, replaces legacy
  `{rail,txHash,kind}` transaction references with the applicable
  `ChainTxRef` arm, and deterministically re-hashes/re-signs the affected
  bundle and settlement fixtures. Adds an exact-shape suite, extended by #315
  to 23 cases, covering all three attestation anchor kinds and all fourteen
  transaction-reference
  discriminators, including nested AP2 receipt attestations and negative
  legacy forms. The manifest gains two golden executable cases but retains
  `dacsVersion: "0.1"` because that is the full-profile baseline identifier,
  not a fixture revision. No normative protocol rule changes.
- **Domain-separator registry golden regenerated** (CORE §B.7; #283) — replaces the stale `sig-registry-closed-16` assertion with a count of 24 and the exact sorted separator set published by the closed registry, including the `dacs-finality-commitment:v1:` separator added by the SR-2 lifecycle work. The manifest validator now compares exact membership, so a future remove-one/add-one substitution cannot pass behind an unchanged cardinality. Refreshes the lifecycle manifest and trace pins. No normative protocol rule changes.

### Clarified — DACS-1 identity and discovery

- **Canonical Demos agent ClaimReference** (§6.3.1 / §A.1; #293) — specifies
  `did:demos:agent:<64-lowercase-hex>` as the self-certifying Demos profile
  under the registered `did` scheme. Clarifies that `demos:0x<64hex>` is
  substrate-address notation, not a registered ClaimReference or reputation
  alias, and must not be emitted in ClaimReference fields.
- **Operational listing reachability** (§6.3.4 / §6.3.6; LP-5; #294) — active
  publishers should maintain at least one actionable machine engagement
  surface. Intent-scoped x402 bases may expose discovery before a job-specific
  402 exists. Catalogs may publish time-stamped reachability hints, but dynamic
  probes never change content/signature validity, conformance, revocation,
  identity, or reputation.

### Fixed — DACS-1 / DACS-4 rail resolution

- **Listing-time accepted-rail validation executes canonical resolution**
  (§6.3.4 / §9.4.3, LRR-1..LRR-6; #298) — `acceptedRails` membership is now
  explicitly a publisher claim, never proof that the rail exists. Every
  pay-bearing listing must independently resolve each accepted rail through the
  authenticated registry, match any pinned version and the signed definition,
  and bind each `pay-*` phase to the registered, same-`railId`-invariant
  `phaseHandler`. A conclusively unknown or mismatched rail rejects;
  missing/unverifiable registry authority or any unavailable advertised
  definition is `indeterminate` and blocks every new session from that listing.
  The reader composes rail resolution into an explicit overall listing
  disposition: ordinary validation failures and rail `rejected` produce
  `rejected`; rail `indeterminate` remains non-accusatory but cannot mask a
  later signer-control failure.
  PA-1 unpinned references select the signed snapshot's unique highest version;
  PA-2/PA-3 readers cannot silently substitute in-code constants. Selection of
  one complete rail reference, session-start pinning, and authoritative
  availability checks still run separately.

### Added — CORE v0.2 / DACS-1..5 lifecycle gates

- **Normative SR-2 transaction lifecycle and portable `AnchorReceipt`** (CORE §5.1, SR2-1..SR2-9) — distinguishes local submission, binding-proved durable acceptance, consensus inclusion, finality, and the rejected/dropped/replaced/expired/reorged lifecycle outcomes. `indeterminate` is a separate observation disposition over a hash-linked preserved receipt, never a lifecycle transition, demotion, or authority to resubmit. Authenticated dropped/expired/reorged transactions may re-enter acceptance/inclusion; receipt conflicts are ordered by binding-authenticated evidence, never observer time. External index visibility is orthogonal and never gates protocol progress. The evidence-carrying portable receipt binds logical/native artifact addresses, content hash, native transaction, writer, nonce, block evidence, and finality profile without recursively requiring the receipt itself to be anchored.
- **Stage-specific anchoring gates** (DACS-1 §6.3.4 LP-1; DACS-2 §7.8 VPC-3/VPC-5; DACS-3 §8.6 CA-1/CA-8; DACS-4 §9.5.1 PC-7 / §9.9 PIPE-6; DACS-5 §10.3.1 ST-3/ST-7/ST-11) — active listings require finalized, resolvable anchors; Vet may progress reversibly on binding-proved durable acceptance; payment and irreversible delivery require the agreement commitment to be finalized; and a successful session remains `audit-pending` until every required artifact and the completed bundle are finalized and independently resolvable. `audit-pending` pauses/resumes through ST-7 on SR-2 failure and can never become a post-payment abort. Bumps **DACS-1 to v0.4, DACS-2 to v0.3, DACS-3 to v0.4, DACS-4 to v0.4, and DACS-5 to v0.4**.

### Fixed — DACS-3 / DACS-4

- **Commitment timestamp circularity removed through a minor-safe new type** (DACS-3 §8.5.2 / §8.6, CA-8/CA-9) — new producers emit the structurally distinct `FinalityCommitmentRecord` (`finalityCommitmentVersion: "1"`, signed under `dacs-finality-commitment:v1:`) with `createdAt` and an explicit orchestrator `signature`; `committedAt` is derived only after anchoring from the finalized receipt's consensus timestamp. The v0.1-v0.3 `CommitmentRecord` and `dacs-commitment:v1:` signed shape remain unchanged and readable for historical audit, with legacy `committedAt` cross-checked against authenticated historical anchor time. Deadline and listing-expiry checks use the applicable authenticated anchor time.
- **Post-payment evidence catch-up generalized to every rail** (DACS-4 §9.5.1 PC-7) — once payment reaches rail-defined finality, delayed SR-2 `SettlementEvidence` anchoring is idempotent asynchronous bookkeeping. It cannot fail or resubmit the payment; it keeps the session non-terminal at the DACS-5 audit gate until evidence finalizes.

### Fixed — documentation

- **Flow-trace signing preimage aligned with CORE §B.7** (#277) — the informative `signedBytes` helper now appends the UTF-8 bytes of the 64-character lowercase artifact-hash string instead of decoding it to 32 raw digest bytes. This matches CORE §B.7 and the published golden Ed25519 signature; an executable regression pins the 80-byte accepted preimage and rejects the former 48-byte construction. No normative protocol rule changes.

### Fixed — DACS-5

- **Legacy three-party fault reconciliation** (§10.4.3 / §10.5.1; #304) — compares legacy copies on implied-fault sets derived independently from each copy's authenticated roster and treats them as divergent only when those sets are disjoint, after the unchanged outcome-class and `phaseSummary` checks. This preserves every buyer↔seller outcome pair, rejects mismatched rosters that would manufacture a common role, and carries a singleton `{orchestrator}` intersection into reputation's orchestrator-neutral exclusion. Legacy/legacy, mixed, and `FaultAttestationBundle` pairs now produce the same neutral reputation for equivalent orchestrator-caused failure and abort cases. `perspective_flip` remains the one-copy scoring rule; bundle bytes, schemas, mixed-version authority, and reputation formulas are unchanged. Adds reconciliation, roster-mismatch, and derivation-parity candidate vectors plus an exhaustive outcome-pair compatibility check.

## [0.4] — 2026-07-27

Interoperability and settlement-hardening release: payee-bound agreements,
metered pricing, AP2 receipt verification, canonical x402 receipt and SB-3
nonce binding, revocation and bundle discovery bindings, absolute fault
attribution, replayable reputation derivation, and expanded executable
conformance coverage. **Version note:** the release tag is the coordinated-cut
counter, decoupled from independently versioned documents — the composition
under this tag is **CORE 0.1; DACS-1 0.3; DACS-2 0.2; DACS-3 0.3; DACS-4 0.3;
DACS-5 0.3**.

### Changed — DACS-5 conformance

- **Legacy bundle reconciliation via `perspective_flip`** (§10.4.3 / §10.5.1; PR #248 round-5, E1–E3) — the "canonically diverge" definition and §10.5.1 guard (ii) now reconcile a legacy `AttestationBundle` pair through the `perspective_flip` mapping before comparison, aligning the consumer definition with the deriver machinery. Perspective-partner spellings (`aborted-by-self` ↔ `aborted-by-other`, `failed-perm` ↔ `failed-counterparty`) are one event and do not diverge; two copies that both blame self genuinely diverge. This corrects the prior outcome-spelling clause, which called perspective partners divergent even though the deriver reconciles them.
- **Mixed-version divergence is implied-fault SET membership** (§10.4.3; E4) — a legacy copy's role-relative `outcome` maps through the §10.4.1 permissible set to an implied-fault SET (a singleton in a two-party session, preserving the prior mapping byte-for-byte). The pair diverges when the `FaultAttestationBundle`'s `faultedParty` is not a member of that set. This makes the rule total for a distinct-orchestrator session, where a legacy `failed-counterparty` / `aborted-by-other` identifies more than one non-R party.
- **Replayable determinism receipt** (§10.5.3 (3)/(4), §10.5.1; E5) — `ResolutionContextEntry` gains `roleEvidence` (an XOR of the verified `BundleBinding` or the pure-mapping anchor address), `counterpartyRef` (REQUIRED for a two-copy jobId, so a rederiver can re-run §10.4.3 divergence and authority selection), and `absenceBinding` (REQUIRED for a one-copy jobId on a write-input substrate). A receipt missing any member REQUIRED for its disposition is non-conforming.
- **BB-6 per-signer fetch budget** (§10.4.2; E6) — the N=8 fetch budget is now per authenticated `signer` (ties broken by ascending `nativeAddress`), so an outsider's flood cannot exhaust the honest role-holder's allocation; the co-signed party-map prune is MANDATORY in a reputation-derivation context, and BB-7 exhaustion is stated per-signer.
- **BB conformance-plan row** (CONFORMANCE-PLAN §14.5 BB row; E8) — replaces the BB-1..BB-8 "resolution vectors pending" placeholder with citations of the shipping security-vector sets.
- **BB-5 check 9 role-match authorization** (§10.4.2; PR #248 round-7) — `resolve_bb6` now authorizes a candidate only when its signer's authenticated role (the co-signed party map, or the anchored bundle roster) equals the role the binding claims, not by key-membership. This closes the cross-role-insider path where an insider party (e.g. the buyer) signs a binding claiming the counterparty's role under a full two-party map.
- **Replay authenticates every copy** (§10.5.3; PR #248 round-7) — `validate_resolution_context` authenticates `bb6Context.partyMap` against the authoritative bundle roster before any authorization use, and re-runs BB-4 + BB-5 checks 1–5 on every candidate binding carried in the context; a forged partyMap or a BB-5-invalid candidate fails the receipt closed.
- **BB-6 same-role ladder executed** (§10.4.2 / §10.5.1; PR #248 round-7) — the intra-signer multiplicity ladder (canonically-equal collapse; full-signature precedence; equal-standing divergence → `indeterminate`) is now EXECUTED in `resolve_bb6`; it was previously asserted only as pinned metadata on the `bundle-binding` vectors.
- **Six new executed BB-6 vectors + pin refresh** (PR #248 round-7) — `outsider-binding-flooding-v0.3` gains `cross-role-insider-binding-pruned` and the three ladder rungs (L1 collapse, L2 precedence, L3 void); `receipt-rederivation-v0.3` gains `forged-partymap-unauthenticated-refused` and `bad-candidate-binding-in-context-refused`. Each is mutation-proved against the pre-fix oracle. Refreshes the four implementation-manifest `manifestSha256` pins after the `MANIFEST.json` summary edits.
- **BB-6 equal-standing two-full-copy void, executed in replay** (§10.4.2 / §10.5.3; PR #248 round-8) — `receipt-rederivation-v0.3` gains `equal-standing-two-full-copies-void-refused`: two divergent full-standing same-role copies at one jobId (co-signed buyer+seller, diverging on the material `outcome` field) are of equal signature standing, so BB-6 selects neither and the side is `indeterminate`; a replay that reconstructs the complete anchored map from every candidate bundle refuses the receipt. Mutation-proved against the pre-fix reconstruction, which resolved `present` from the claimed-winner-only anchored map.
- **BB-5 checks 2-5 completed on replay candidates** (§10.4.2 / §10.5.3; PR #248 round-8) — `verify_binding` now enforces BB-5 check 3 (`bindingVersion` in the consumer's supported set) and check 5 (`logicalAddress` == derive(signed `jobId`, `role`)) on every candidate, alongside the existing BB-4 + check 4 (`jobId`/`role`). Checks 2-5 now execute on candidates; **check 1 (discovery-surface resolution) is not replayable — replay consumes the deriver-supplied `candidateBindings`, the disclosed §10.5.3 completeness residual (#251)**. This completes the coverage the round-7 "checks 1-5" note claimed but only partially shipped (BB-4 + check 4).
- **Genuine BB-5 check-8 byte recompute in replay reconstruction** (§10.4.1 / §10.4.2; PR #248 round-8) — `validate_resolution_context` now reconstructs the COMPLETE anchored map `resolve_bb6` requires: it dereferences every candidate bundle and recomputes its §10.4.1 `attestation_bundle_hash`, comparing byte-for-byte against the binding's `bundleContentHash` (a genuine recompute in the reconstruction loop — distinct from `verify_binding`'s claim-to-argument check-8, which is unchanged). The claimed-authoritative copy is byte-revalidated the same way. Boundary: an unfetchable or null-native candidate, an absent required `budget`, or a bad-winner copy REFUSES the receipt (fail-closed); a fetched candidate failing the check-8 recompute or post-fetch role (check 9) is INERT — dropped, never counted toward collapse/precedence/void. Closes the round-7 replay reconstruction that anchored only the claimed winner (`{native: auth}`), which read every competing copy as non-full BY ABSENCE. Mutation-proved: `equal-standing-two-full-copies-void-refused` (2-full ⇒ refuse), `unfetchable-competitor-candidate-refused` (unfetchable ⇒ refuse), `poisoned-candidate-bundle-inert-honest-resolves` (bad-hash ⇒ inert, honest resolves).
- **Scope: replay signature authentication is crypto-conditional** (§10.5.3; PR #248 round-8) — `verify_binding` returns success WITHOUT cryptographic verification when `pubkeys` is None or `cryptography` is unavailable; every "replay authenticates candidate bindings" statement is scoped to the crypto-enabled invocation, not asserted as an unconditional invariant. CI installs `cryptography` fail-closed (pinned) so the reference run verifies; the function contract itself does not enforce it.
- **BB-6 mandated ordering contract executed in replay reconstruction** (§10.4.2 / §10.5.3; PR #248 round-9) — `validate_resolution_context` now runs the §10.4.2 (L381) order end-to-end: authenticate `bb6Context.partyMap` against the authoritative roster (N5) → **role-holder-strict prune** on the entry's `resolvedRole` (keep a candidate iff its signer's MAPPED role is the side being resolved — the signer's authenticated role, NOT the candidate's claimed-role field, so a role-holder-signed wrong-role candidate survives and still refuses via re-verification: N6 preserved) → BB-4 + BB-5 checks 2-5 re-verification on SURVIVORS only → recorded-budget BB-7 exhaustion (side-level; an over-budget signer bucket makes the side `indeterminate` with zero fetch work spent) → ascending `(bundleContentHash, nativeAddress)` fetch → FULL BB-5 post-fetch validation of every fetched copy (check 7 `jobId`; full check 9 — signer holds the claimed role, `anchoredByRole` consistency, §10.4.1 fault-permissible set, §10.4.1 bundle-signature validity + required-signer; check 8 byte recompute), a copy failing any check DROPPED inert → the BB-6 ladder over the surviving VALID bindings and their validated bundles only. The normative text (L381) already mandates this order; round 9 closes the reference reconstruction and the executable test surface to it. Test surface: 7 executed methods in `tests/test_round9_ordering_contract_vectors.py` (R1, R2, R3, F1, F2, F4, F5), each driving the full replay entry point crypto-on; suite 194/0/0/0; the round-9 mutations M1-M8 are each killed by exactly their target predicate.
- **Correct-the-record — round-8 reconstruction closure was overstated** (PR #248 round-9) — round 8 claimed `validate_resolution_context` "reconstructs the COMPLETE anchored map `resolve_bb6` requires" and that it "Closes the round-7 replay reconstruction that anchored only the claimed winner". Review found three same-class holes still live at that head (`f11adda`) — the class being an unfiltered / pre-capped / unvalidated candidate reaching the BB-6 ladder: **R1** — a copy dropped from `anchored` (check-8 fail) re-entered the ladder because the unfiltered `candidateBindings` list, not the surviving valid set, was handed to `resolve_bb6`, re-authorizing the inert copy off the partyMap at lesser standing; **R2** — candidates were dereferenced BEFORE the mandatory partyMap prune, so an unauthorized candidate consumed fetch work (and could refuse an honest receipt); **R3** — post-fetch validation omitted check 7 and the full check 9, so a winner with `anchoredByRole` flipped seller→buyer or with invalid bundle-signature bytes was accepted. Each was reproduced RED at `f11adda`, fixed by the reordering above, and mutation-pinned. The first round-9 reordering itself introduced a transient regression (see F1) — likewise caught, reproduced, and fixed under the same discipline round 8 applied when it corrected round 7's premature "checks 1-5" claim.
- **Execution self-audit findings F1-F5 — F1/F2/F4/F5 each reproduced as a committed red test before its fix; F3 a documentation gap** (PR #248 round-9) — spec-text conformance reads plus executed probes surfaced five defects: **F1** (round-9 transient) — the first reordering pre-capped per-signer candidates to `[:budget]` before handing them to `resolve_bb6`, masking BB-7 recorded-budget exhaustion — the replay/live budget-equivalence property the round-8 review verified at the prior head; caught by the audit before external review, now reproduced via the existing `"BB-6 re-selection differs (got 'indeterminate'/None, …)"` path with no new reason string; **F2** (pre-existing) — the candidate re-verification ran before the prune, so an outsider's malformed candidate could sink an honest receipt; **F4** — the interim signer-membership prune let a MAPPED counterparty's cross-role candidate reach the authorized-unfetchable "not dereferenceable" refusal; **F5** (compound) — a cross-role flood inflated the per-signer count, firing an exhaustion route that resolved via `anchored={}` and thereby BYPASSED post-fetch validation, resurrecting the poisoned-copy leak on that route. F2/F4/F5 share one root cause — pruning by signer-membership — closed by the role-holder-strict predicate on `resolvedRole`. **F3** — `resolve_bb6`'s `anchored` precondition (every bundle passed in MUST already have passed full BB-5 post-fetch validation, incl. §10.4.1 signature validity, so signature standing is never computed on an unvalidated copy) is now documented. Fixture honesty: the first `F4b` reproduction built the fetchable cross-role bundle with arguments identical to the honest copy, so the deterministic builder produced the SAME content hash — making its "cross-role hash not fetched" assertion unsatisfiable (the honest hash is always dereferenced as the authoritative copy); the collision was caught when the fix went green, and corrected with a distinct hashed outcome plus an inline `assertNotEqual(xh, h)` distinctness guard. Re-verified at this head: the shared-`nativeAddress` last-write order-invariance flagged in the round-8 review — two candidates sharing one native with divergent content hashes resolve to the same disposition/refusal and the same `anchored` map under both candidate orders (the pre-fetch sort makes the overwrite order-independent). Unchanged residuals: the §10.5.3 candidate-set completeness residual — replay consumes the deriver-supplied `candidateBindings`, not a discovery-surface re-scan (#251); and `deref` exceptions other than `None`/`KeyError` propagate rather than being caught as unfetchable (round-8 review note).
- **Correct-the-record — round-9 "FULL BB-5 post-fetch validation" over-stated the §10.4.1 signature facet** (§10.4.1 / §10.5.3; PR #248 round-10) — round 9 stated the replay reconstruction runs "FULL BB-5 post-fetch validation of every fetched copy (… §10.4.1 bundle-signature validity + required-signer …), a copy failing any check DROPPED inert". That §10.4.1 signature facet was narrower than named: it enforced only presence of the anchoring role-holder's signature, plus a per-party verification that first keyed the carried signatures into a `{party: signature}` dict — so validity was checked against the LAST entry per party, not every carried entry. Five gaps passed the claim, each reproduced as an executed probe before any fix: (1) **required-signer set** — a non-abort copy (`completed` / `failed-perm` / `failed-counterparty` / `failed-substrate`) was admitted with only the anchor role-holder signed, though §10.4.1 (:322) requires buyer + seller (+ distinct orchestrator) and §10.5.1 (:475/:798) drop a single-signed non-abort copy; (2) **order-dependent dedup** — the party-keyed collapse was last-wins, so a duplicate seller entry carrying one valid and one invalid signature had its surviving (checked) entry selectable by append order (invalid-then-valid passed; valid-then-invalid refused); (3) **algorithm label unchecked** — valid ed25519 bytes relabelled `ecdsa-secp256k1` verified anyway; (4) **non-canonical SIG-6 value** — a `=`-padded Base64URL signature value was accepted (the lenient decoder tolerates padding) with no canonical check before verification; (5) **budget ingress** — a non-integer `bb6Context.budget` (`"8"` / `None` / `8.0`) raised a `TypeError` from the exhaustion comparison or the per-signer slice rather than refusing.
- **§10.4.1 required-signer + signature-value predicate upgraded, type-agnostically** (§10.4.1 / CORE §B.7; PR #248 round-10) — `_bundle_signatures_valid` now applies an **outcome-dependent required-signer set to BOTH bundle types** — the legacy `AttestationBundle` and the `FaultAttestationBundle` (spec :475/:798 state the single-signed non-abort rejection without distinguishing the two; the `outcome` enum is common): a non-abort outcome requires buyer + seller (+ distinct orchestrator) all signed, while an abort outcome (`aborted-by-*`) MAY be single-signed, floored on the anchoring role-holder, for either type. Every carried signature is validated over the **RAW list** (no party-keyed collapse), so duplicate entries for one party are each verified and the append-order dependence is removed; each entry is dispatched on its **`algorithm` label** against a supported set (`{ed25519}`) and passes a **strict SIG-6 canonical decode/re-encode check before cryptographic verification** on both the `FaultAttestationBundle`-signature surface and the `BundleBinding`-signature surface (`verify_binding`). `validate_resolution_context` validates **`bb6Context.budget` at ingress** — a non-int, bool, or `< 1` value is a deterministic `(False, [reason])` refusal, never an exception. FAB reason strings are byte-identical across the round; the legacy path emits the same format. Test surface: `tests/test_round10_validation_predicate_vectors.py`, 21 executed methods crypto-on, each reproduced red against the pre-round-10 head and green after; ten mutations (M-F1, M-F2, M-F3, M-F4a, M-F4b, M-F5, M-G, M-S, M-F1L, M-F1O) each killed by exactly their paired method; the attributability controls (fully-signed, single-valid-per-party, correctly-labelled, unpadded, `budget = 1`, and the legacy fully-signed and single-signed-abort copies, and the orchestrator claim-equality and fully-signed copies) are kept as permanent fixtures beside each defect. At the replay entry point an under-signed winner refuses via the round-9 BB-6 inert-drop route (the existing `"BB-6 re-selection differs (got 'indeterminate'/None, …)"` reason — no new reason string); the signer-naming §10.4.1 reason surfaces at `_post_fetch_valid`, and the committed R10-1 regression pins both surfaces.
- **Structural-refusal contract on untrusted receipts** (§10.5.3; PR #248 round-10, **corrected round-11**) — round 10 stated `validate_resolution_context` "upholds its `(ok, reasons)` return on an arbitrarily malformed receipt rather than raising", shipping a **pre-loop `resolutionContext` array check**, a **per-entry structural gate** (type-when-present for `roleEvidence` / `bb6Context` / `partyMap` / `candidateBindings` and its elements / `counterpartyRef` / `counterpartyRoleEvidence` / `absenceEvidenceRef`), a **deref'd-copy shape validation applied to the winner AND the counterparty**, and a self-contained sweep of 21 execution-proven exception escapes. **Corrected, round 11**: that evidence established **exception containment over the enumerated member set only** — independent round-11 probes found **fail-open and exception paths beyond it**: empty or unknown `roleEvidence.kind`, unknown or absent `counterpartyDisposition`, non-string `contentHash` / `counterpartyRef.contentHash` / `absenceEvidenceRef.contentHash` deref keys, nested `BundleBinding` members reaching a string concat / `in`-frozenset / `dict.get`, and a shape-malformed fetched candidate copy. **What is now demonstrated (round 11, in the round-11 entry below):** deterministic refusal — no exception escape, no fail-open — over the §10.5.3 `ResolutionContextEntry` grammar (presence + vocabulary + member types, spec :535-552), witnessed by a deterministic (path × mutation) oracle of **65 grammar paths × 7 mutation kinds → 227 applicable grammar-violating cells, every cell refusing** (plus **4 grammar-legal cells recorded**); a `BundleBinding` **structural ingress** typing every required member pre-crypto in **both** modes at **all four** `verify_binding` call sites (signature VERIFICATION unchanged, crypto-gated); the deref'd **winner and counterparty** copies refusing via the round-10 shape gates; and a shape-malformed **fetched candidate** copy **dropped inert** (R1/R3 semantics), never a refusal, never a raise.
- **Documented boundaries — recorded, not closed this round** (§10.4.3 / §10.5.3; PR #248 round-10; (iii) round-11) — three boundaries are recorded rather than silently carried. (i) The **counterparty copy is bounded by the §10.4.3 divergence check and by verification of its role-evidence binding, but is NOT byte-revalidated** against `counterpartyRef.contentHash` the way the claimed-authoritative winner is (the winner is recomputed and refused on mismatch; a counterparty whose bytes do not hash to the committed ref, yet which is canonically equal to the winner, is accepted) — a recorded asymmetry and follow-up candidate. (ii) The `deref` / `evidence_deref` callables are **trusted to return parsed JSON**; a non-JSON value (e.g. Python `bytes`) is a caller contract violation surfacing as a `TypeError` inside `bundle_hash` canonicalisation, not a receipt-reachable path (a receipt cannot inject a non-JSON value through the content-addressed store). (iii) The **address-arm role-SEGMENT semantic check** is recorded, not closed: on a `roleEvidence` / `counterpartyRoleEvidence` `{kind:"address"}` substrate, spec :540 requires the `resolvedAddress` **role segment to equal `resolvedRole`** — round 11 validates the arm's **structural shape only** (`resolvedAddress` present and a string), not that role-segment relation.
- **Receipt-ingress class closure over the §10.5.3 entry grammar** (§10.5.3 / CORE §B.7; PR #248 round-11) — three surfaces in `tests/dacs5_reference.py` close the fail-open and exception paths round 10's structural-refusal note did not reach. **(A) Per-entry grammar gate** (`_entry_structural_gate` + `_role_evidence_grammar`) now enforces presence + vocabulary + member types for the §10.5.3 `ResolutionContextEntry` grammar (spec :535-552): `contentHash` a required string; `roleEvidence` / `counterpartyRoleEvidence` on the `{"binding","address"}` XOR; `counterpartyDisposition` in `{"present","absent"}`; `counterpartyRef.contentHash` and `absenceEvidenceRef.{kind,locator,contentHash}` required strings — with **`candidateBindings` deliberately kept type-when-present** so BB-6 outsider garbage stays prunable-**inert** and can never refuse an honest receipt (F2/F4, spec BB-6 :381/:387; its members are validated at BB-4/BB-5 re-verification, which only role-holder survivors reach). **(B) `BundleBinding` structural ingress** in `verify_binding`, run pre-crypto in **both** modes at all four call sites, types the required members (`bindingVersion` / `jobId` / `role` / `signer` / `nativeAddress` / `bundleContentHash`; `signature` an object; its `signer` / `algorithm` / `value` strings) before any reaches a concat / `in`-frozenset / `dict.get`; signature VERIFICATION is unchanged and stays crypto-gated. **(C) Fetched candidate copy** — the third deref site, previously not shape-gated (winner and counterparty already were) — is now `_bundle_shape_ok`-checked and, on failure, **dropped inert** (R1/R3a/R3b), never a refusal. Round-10 D6 gate reason strings are byte-identical. Witness: a deterministic **(path × mutation) grid oracle** — 65 grammar paths × 7 mutation kinds → **227 applicable grammar-violating cells, every cell refusing**, plus 4 grammar-legal cells recorded and the four round-11 reviewer probes asserted present as grid ids (and kept as named exact-reason regressions). **Mutation-pin campaign:** 19 deliberate guard breaks, each killed by a paired exact-reason regression; three initial pin gaps were found and closed — **MA3** (the grammar-gate binding-arm object check was backstopped by `verify_binding`, so its removal killed no test → new exact-reason pin on the gate's own reason), **MB9** (the ingress `signature.signer` / `.value` string checks were backstopped by the BB-4 signer-mismatch and `sig6_canonical` → new exact-reason ingress pin, recording that `.value` typing is load-bearing for structural-mode callers since `sig6_canonical` is crypto-gated), and **MC2** (the drop-inert control witnesses the round-9 `_post_fetch_valid` drop layer, not the round-11 shape gate → the shape-gate drop-vs-refuse is re-paired to the `pf_sig` / `pf_par` inert-accept asserts). Suite 243/243, zero fail/error/skip; the reference predicate is unchanged by the pin closures.
- **Integrated-replay completeness gate + `resolvedRole` vocabulary enforcement** (§10.5.3; PR #248 round-12) — two executable replay blockers @randomblocker reproduced outside the round-11 grid are resolved. **(B1) Integrated-replay completeness.** `receipt_required_members_present` is now invoked inside `replay_receipt` (after the CORE §11.1.2 discriminator gate, before `validate_resolution_context`) and hardened to refuse deterministically — never raise — over: `resolutionContext` REQUIRED as an array (spec :532); each entry an object carrying a string `contentHash`; `bundleRefs` REQUIRED as an array (spec :531); `resolutionContext` length equal to `bundleRefs` and keyed to it in canonical order (spec :850-854); and `metrics` (object) + `bundleCount` (integer) present — the two top-level members `replay_receipt`'s byte-identity compare consumes. `validate_resolution_context` likewise now refuses a missing `resolutionContext` with its own reason (the existing non-array reason is unchanged). Before this, a receipt with a missing / empty / miskeyed / order-mismatched context replayed `True` or raised: an empty `resolutionContext` against non-empty `bundleRefs` (with matching empty-derivation metrics) returned `(True, bundleCount 0)`; a context reordered relative to `bundleRefs` replayed `True` because `derive()` re-sorts; a missing `resolutionContext` / `metrics` / `bundleCount` raised `KeyError`. **Two behaviour changes to `receipt_required_members_present`:** an ABSENT `resolutionContext` now refuses EVEN when `bundleRefs` is empty — the conforming empty form is an empty array, not a missing member (prior behaviour returned `(True, [])`); and malformed shapes (`resolutionContext` not an array, a non-object entry, a non-string `contentHash`) now return `(False, [reason])` instead of raising `TypeError` / `KeyError`. **(B2) `resolvedRole` vocabulary.** `_entry_structural_gate` now validates `resolvedRole` against `{"buyer","seller"}` (spec :537) BEFORE branching on evidence kind, so an invalid or missing enum refuses on BOTH the binding- and address-backed arms. Previously an invalid `resolvedRole` validated on the address arm (consumed only as `_other(role)`), and on the binding arm was caught incidentally by the later `binding.role` comparison under a different reason. **Grid.** The round-11 (path × mutation) oracle gains a `present-addr` base — both role-evidences on the address XOR arm (spec :548 "same XOR shape as roleEvidence", :540) with `bb6Context` removed and a valid role — exercising the counterparty address arm: **227 → 233 grammar-violating cells**, the 6 new cells refusing; `absenceEvidenceRef.kind` stays an open string (spec :551), not a closed union, so it carries no vocabulary arm. **Witness:** `tests/test_round12_replay_completeness_vectors.py` — 17 tests: T1–T10 red at `82960ce` with controls green and green after; two rrmp entry-shape pin-closures (T11 a non-object entry, T12 an object lacking a string `contentHash`); two further never-raise closures on the gate's own reads (a non-array `bundleRefs`; a type-guarded `roleEvidence` read deferring object-typing to the round-11 grammar gate) and the `bundleRefs` absence-vs-empty sibling, found by reviewer-lens self-audit before external review (T13/T14); a non-list-context pin (`M3_nonlist`); and 2 controls, with T7 additionally carrying a crypto-path assertion. A D3 mutation campaign breaks the gate seventeen ways (M1–M17), each killed by its paired construction — M11 (the enum gate accepting a missing role) kills T8/T10 one per arm; M12/M13 kill the T11/T12 entry-shape pins by raise; M14 (removing `verify_binding`'s `role` ingress type-check) kills `coll2b`, whose reason shifts to the role-equality backstop; M15/M16 kill T13 (bundleRefs reason-shift / len-raise) and M17 kills T14 (raise). M9 (deleting the enum gate) re-points the round-11 `coll2` regression, whose colluding non-string enum is now caught by the B2 gate first; `coll2b` preserves that `binding.role` ingress type-check on the valid-role arm. The address-arm role-SEGMENT semantic relation (spec :540) is unchanged — the disclosed residual (iii), handled independently.
- **Explicit-null `signature` members refuse the always-on structural typing** (§10.4.2 / DACS-4 §9.7; PR #248 round-13) — `verify_binding`'s structural member gate now requires `signature.signer` / `signature.algorithm` / `signature.value` to be **present strings whenever a `signature` object is present** (the pre-fix `is not None and not isinstance` exemption dropped), on BOTH the structural (`pubkeys=None`) and crypto paths and at all four `verify_binding` call sites — including the integrated `validate_resolution_context`. An **absent** `signature` is unchanged: it skips the member gate and still refuses at the `signature.signer == binding.signer` equality (`{}.get("signer")` is `None` ≠ the required-string signer). Reviewer origin: @xm33 round-12 Blocker 1 + @randomblocker, independently reproduced at `02a0e89`. Pre-fix defect: a null `value` (or `algorithm`) cleared the documented always-on typing and reached `ok: True` on the `pubkeys=None` path — because `sig6_canonical` / algorithm-dispatch are crypto-gated — so a receipt validated structurally with a meaningless signature member. **Scope:** this is the structural member-TYPING gate; signature VERIFICATION is unchanged and stays crypto-gated (structural mode authenticates no signature — not a forgery/authority claim). **Witness:** `tests/test_round11_receipt_ingress_vectors.py` — explicit-null per-member pins (exact member-gate reason `binding.signature.<m> must be a string (got NoneType)`, on both tiers × both modes; `signer` pinned on the member-gate reason SPECIFICALLY because the `:194` signer-equality is a live backstop that would keep a refuse-only signer pin green on revert — the round-12 `coll2` UNREACHABLE class), red on the B1 revert; the absent-signature invariant separately pinned (stays green on revert — a different invariant). The prior scalar pin `test_r11_signature_member_scalar_defect` (member=`123`) could not reach the null sub-case (123 is caught by the old guard) and runs crypto-only — the reachability gap these pins close. D6 class-sweep of the `is not None and not isinstance` idiom impl-wide: the `bb6Context.candidateBindings[k]` null sub-members (`:970`) proven INERT by execution (pruned / re-verified before use), the remaining sibling sites justified. Suite 271/0/0/0 at B1 completion.
- **Boolean and duplicate `phaseSummary` index refuse at the shape gate, before the divergence keying** (§10.4.3 / §10.5.1; PR #248 round-13) — `_bundle_shape_ok` now (Limb A) rejects a **boolean** `phaseSummary` index with its own reason (`phaseSummary[k].index must be a non-boolean int or string (got bool)` — matching the `bundleCount` :834 / `budget` :1122 bool-exclusion idiom, since `isinstance(True, int)` is `True`) and (Limb B) rejects a **duplicate/colliding** index via a running `seen_idx` set (`phaseSummary[k].index is a duplicate/colliding index <v>`), BEFORE `_phase_summary_diverges` keys `{e["index"]: e}` and silently last-write-wins. The gate runs on BOTH the winner AND counterparty copies, which are `_bundle_shape_ok`'d before `divergence()` on the receipt path, so a malformed copy refuses upstream and `_phase_summary_diverges` stays a pure bool over well-formed input. Reviewer origin: @xm33 round-12 Blocker 2 (helper-tier grid; he flagged receipt-tier composition NOT executed) + @randomblocker (crypto-on full-path); the receipt tier is reproduced this round — pre-fix the contradictory job stays in `bundleCount` (counted into §10.5.1 metrics) instead of excluded. Pre-fix defect: a bool index (`True == 1`, `hash(True) == hash(1)`) and a duplicate int index both collapse the keyed compare, masking a §10.4.3 divergence, so `divergence()` returns `False` and the divergent job is aggregated. **Scope:** `derive()` — the producer — does NOT shape-gate; the consumer defense is `validate_resolution_context` / `replay_receipt` refusing the whole receipt (the producer's own `bundleCount` is outside the defended surface). **Witness:** `tests/test_round11_receipt_ingress_vectors.py` — `test_r13_b2_bool_index_defect` (bool / Limb A, both modes + `replay_receipt` refusal), `test_r13_b2_dup_index_defect` (pure int-1 duplicate / Limb B; reachability lock: post-fix a bool can no longer reach `_phase_summary_diverges` → no bool-on-dup-limb pin), and `test_r13_b2_genuine_divergence_control` (a non-colliding divergent pair still refuses AND `derive()` excludes the job, `bundleCount 0`). Three-revert independence proven (Limb-A revert reddens only the bool pin, Limb-B only the dup pin, both reverts leave the control green); the round-10 NoneType-index pin's exact reason (`… index must be an int or string (got NoneType)`) is deliberately preserved by giving `bool` its own branch. Pattern-propagation sweep: the Class-A int gates are all bool-excluded (`bundleCount` / `budget` / index); the party/signer keyed-collapse sites (`roster` / `_full_standing` / `_bundle_signatures_valid`) proven caught-elsewhere by execution (byte-committed roster + per-signature RAW-list verify + round-9 post-fetch scoping); the `scripts/run_lifecycle_walkthrough.py` divergence sibling is out of the validator surface (self-built pipeline indices). Suite 274/0/0/0 at B2 completion.
- **`windowingBasis` required + vocabulary-checked; `sr2-anchor-timestamp` fails closed** (§10.5.3 / §10.5.1; PR #248 round-13) — the §10.5.3 determinism receipt's `windowingBasis` (spec :530/:854/:856/:954) is now enforced at the replay ingress. `receipt_required_members_present` **REQUIRES** the member and validates it against the closed two-literal vocabulary `{finalisedAt, sr2-anchor-timestamp}`: an absent basis refuses (`windowingBasis is REQUIRED (spec :530)`) and an out-of-vocab value refuses (`windowingBasis must be one of ['finalisedAt', 'sr2-anchor-timestamp'] (got …)`) — two distinct reasons — and the prior silent `derivation.get("windowingBasis", "finalisedAt")` default in `replay_receipt` is removed. `sr2-anchor-timestamp` is a valid literal (it PASSES the vocab gate) but the SR-2-anchor-timestamp windowing clock is a §10.5.1 **SHOULD** (spec :832/:1010) **NOT implemented by this reference**, so it **fails closed** with a THIRD distinct reason: `derive()` refuses `basis="sr2-anchor-timestamp"` (`windowingBasis 'sr2-anchor-timestamp' is not implemented (fail-closed; §10.5.1 sr2 windowing is a SHOULD, not implemented by this reference)`) instead of the prior record-but-hardcode path that recorded `sr2` while windowing on `finalisedAt`, and `replay_receipt` refuses an `sr2`-declared receipt (→ `(False, None)`) rather than claiming byte-identical reproduction *under the recorded basis* (§10.5.3 (3) :854 / replay :581) that never happened. The `finalisedAt` basis is unchanged — computed, recorded, and replayable end-to-end. **Disclosed gap:** SR-2-anchor-timestamp windowing remains unimplemented; the differential two-clock regression (two bundles falling on opposite sides of the `finalisedAt`-vs-`sr2` clocks selecting different sets) is deferred to implementation. Reproduced red-first (silent-default accept, out-of-vocab accept, record-but-ignore, replay-under-wrong-clock), then closed with the `finalisedAt` control still replaying byte-identical.
- **Adopts @randomblocker's round-14 closure patch: three fixes + a replay interface change** (§10.4.2 / §10.4.3 / §10.5.3; PR #248 round-14) — (1) **`windowingBasis` container-raise fixed**: `derive` and `receipt_required_members_present` now guard `isinstance(basis, str)` *before* the frozenset membership test, so a `[]`/`{}` basis refuses deterministically (`windowingBasis must be one of ['finalisedAt', 'sr2-anchor-timestamp'] (got …)`) and `derive` raises `ValueError` instead of the prior `TypeError: unhashable type` (this was @mj-deving's published cross-run trigger). (2) **BB-6 full-signature precedence reads across EVERY validated copy in a form**, not only the lowest-address `cps[0]`, so a full-standing copy is selected independent of native-address ordering (@mj-deving / @cX3po). (3) **Counterparty copies now receive full §10.4.2 post-fetch validation** — including the `anchoredByRole` cross-check and required-signer set — via an anchor-address dereference, closing the unhashed-`anchoredByRole` masking of a §10.4.3 divergence refusal (@cX3po). **INTERFACE CHANGE:** authenticated replay (`validate_resolution_context` / `replay_receipt`) now takes a caller-supplied `anchor_deref` (and optional `pure_mapping_resolver`) and **FAILS CLOSED without one** — a content-hash lookup is ambiguous because signatures and `anchoredByRole` sit *outside* the bundle hash; the round-9/10/11/12 test harnesses are re-threaded to supply it. **Strengthening (spec-silent, disclosed):** the BB-6 single-form collapse case now reports a full-standing copy's address (§10.4.2 is silent on the reported address when copies collapse; aligned with @cX3po's tie-break). **Residual (byte-preserved):** the content-hash `deref` positional is now compatibility-only inside `validate_resolution_context` (every winner/candidate/counterparty/tagged fetch routes through `anchor_deref`); the absence arm is unchanged and remains `evidence_deref`-driven — AbsenceEvidence is hashed whole, so its content-hash lookup is unambiguous and anchor deref is neither used nor needed there. Reproduced red-first by three independent hub probes (container `TypeError`; full-standing-sorts-last → `indeterminate`; `anchoredByRole` flip → wrongly passes), all green after the patch; the hub added six `_post_fetch_address_valid` sub-check mutation pins, one `_deref_role_copy` `KeyError`-scope disclosure pin, a full crypto-enabled `replay_receipt` regression for the counterparty `anchoredByRole` finding, a BB-6 two-full-standing-forms `indeterminate`-void pin, and a binding-arm winner exact-anchor-copy pin (the winner-side analogue of the counterparty fix). This head repairs the `windowingBasis` blocker.

### Added — DACS-5 conformance

- **`FaultBundleExtendedPointer`** (§10.4.2; E7) — a fault-typed extended-pointer form (`faultBundleVersion` discriminator, `dacs-fault-bundle-pointer:v1:` signature domain, registered in the CORE §B.7 SIG-4 table). Normative order rule: `binding.bundleContentHash` == `pointer.fullBundleContentHash` == the recomputed §10.4.1 hash of the dereferenced full bundle — three values, one identity. A signature failure or dereferenced-hash mismatch is rejected content (BB-7), never absence.
- **Executable round-5 vector sets + reference predicates** — adds `tests/dacs5_reference.py` (perspective_flip, implied_fault_set, divergence, resolve_bb6, derive, replay_receipt), imported by the four blocker tests, which now EXECUTE the predicates rather than assert fixture metadata. New/expanded vectors: `mixed-version-reconciliation-v0.3` (+ genuine-divergence, 3-party orchestrator pairs), `outsider-binding-flooding-v0.3` (+ worst-order, distinct-key sybil), `receipt-rederivation-v0.3` (real signed content + missing-counterparty-ref), and the new `fab-bundle-extended-pointer-v0.3` set.

### Added — conformance reporting

- **Scoped implementation claims and `ImplementationManifest`** (PROFILE, §14.10 IM-1..IM-8; #260) — preserves unqualified “DACS v0.1 conformant” as a full-profile claim while defining optional, machine-readable module, role, capability, and experimental claims. Capability reports separate implemented support, operational availability, and deterministic test status. Reports pin the exact specification and conformance-manifest revisions, and distinguish live-only evidence from deterministic conformance. Experimental or unsupported capabilities cannot construct a passing claim. Adds a normative JSON schema, four examples, and dependency-free CI validation. Pin validation now resolves the declared Git revisions, derives document versions and rule references from the pinned profile commit, and verifies the byte-exact suite manifest and case IDs at the pinned suite commit; nonexistent or internally inconsistent pins fail. The profile also records the current release-line module composition without renaming the full-profile `DACS-v0.1` claim. The manifest is self-asserted reporting metadata and never bypasses registry availability, substrate preflight, artifact verification, or another protocol obligation.
- **Minimum-conformant lifecycle walkthrough** (#261, #269) — adds a deterministic five-stage reference path plus negative enforcement cases for listing admission, vet gating, agreement commitment, settlement, and terminal bundle verification. It is executable implementation guidance and conformance support; it does not add a normative protocol rule.

### Fixed — universal signature encoding

- **Canonical signature `value` wire encoding — SIG-6** (CORE §B.7; DACS-1..5 signature envelopes; #263) — requires DACS-owned signature-envelope values to use RFC 4648 Base64URL without padding. Producers emit one spelling; verifiers reject standard-Base64 `+`/`/`, `=` padding, whitespace, impossible lengths, invalid residual bits, and other non-canonical forms before algorithm-specific verification. Composed-protocol fields such as SIWD wallet signatures and Solana transaction signatures retain their upstream encodings. Pre-ruling Base64/hex values may be converted only through an explicitly selected legacy importer that preserves the raw bytes and emits canonical Base64URL; they are not silently accepted by the conforming verification path. Adds distinguishing candidate vectors and executable tests.

### Fixed — DACS-5 conformance

- **Legacy reputation goldens aligned with authoritative-absence guard** (§10.5.1 guard (iv), §14.5; #264) — retires 14 positive pre-guard outputs from the current golden surface because their six one-copy input bundles carry no retained two-address read dispositions. The signed fixture remains byte-stable and its historical metric outputs remain candidates for regeneration with valid resolution context; they are not grandfathered as current conformance. A new golden case and executable predicate pin the current result: raw one-copy inputs without authoritative-absence context produce `bundleCount = 0`, empty references/array metrics, and null scalar metrics. The three negative-by-design reputation cases remain golden. No normative protocol rule changes.

### Fixed — DACS-1 conformance

- **Listing preserve-unknown vectors** (CORE §B.7 SIG-3/SIG-5, §11.1.2, DACS-1 §6.3.4; #247) — turns the existing additivity contract into a signed, portable Listing test without changing normative semantics. A Listing with an inert unknown top-level field verifies when hashed as received; mutating or removing the field after signing fails. A validly signed unknown phase kind remains unsupported, preserving the closed action-discriminator boundary. This exposes closed top-level-key allowlists as non-conforming while retaining required-field and known-phase validation.

### Fixed — DACS-4 / DACS-5 propagation

- **Final settlement evidence hash propagation — FP-1..FP-4** (§9.7; #244) — replaces the unreachable QA criterion that “only payment evidence may differ” after a real settlement transaction becomes known. Placeholder transaction/receipt values are draft-only and cannot be signed, anchored, or referenced by a terminal bundle. Final rail data rebuilds and re-signs `SettlementEvidence`; its final reference and any duplicated phase transaction references propagate into `AttestationBundle`, whose hash and required signatures are recomputed. A re-derivation checker accepts exactly that transitive integrity closure and rejects stale propagation or unrelated semantic changes. No artifact shape or version changes.

### Fixed — DACS-3

- **Commitment authority separated from physical anchor ownership — CA-6/CA-7** (§8.6; #245) — pins the authenticated session orchestrator as the protocol authority for the commitment phase while making the SR-2 transaction submitter, StorageProgram deployer/owner, and native address non-authoritative for buyer/seller role or agreement authorship. Consumers bind the commitment by verifying the orchestrator signature, verifying the agreement's required party signatures, recomputing `agreementHash`, and matching it to the `CommitmentRecord`. An optional separately anchored agreement copy remains valid under any physical deployer/owner because its content hash and party signatures, not substrate ownership, establish the agreement. This clarifies the existing step 5 and CA-4 contract; it changes no artifact shape and adds no DACS-3 version bump.
- **Same-bidder sealed-envelope commit authority — SE-9** (§8.4.3; #209) — makes the earliest valid in-window SR-2-anchored commit for each authenticated `bidderClaim` authoritative, with ascending lowercase-hex `bidHash` as the total-order limb when anchor timestamps are equal. Exact duplicates collapse; every later same-bidder commit is inert, and a reveal that opens only a non-authoritative commit is excluded as a bidHash mismatch. An unresolvable otherwise-candidate anchor leaves that bidder's authority `indeterminate`, never silently selects another commit. This prevents a bidder from anchoring multiple bids and choosing the favourable one at reveal time, and prevents implementations from deriving different winners by choosing earliest, latest, or reject-all. Adds four candidate security vectors; no schema change and no additional DACS-3 version bump beyond the already-unreleased v0.3 surface.

### Fixed — DACS-1 addressing

- **Demos listing address mapping and CF-4 conformance cleanup** (§6.3.4, CORE §B.1; #237) — makes `storageProgramName` explicitly implementation-defined and opaque: DACS requires a Demos-valid colon-free name but does not define a reversible logical-address encoding, because the native `stor-` handle already depends on write inputs and consumers resolve through the required published logical→native binding. Removes the unreproducible 64-hex `addressing.native` output and stale DACS-VERIFY-0003 observation from the golden set; the current substrate rule is `first40hex` and the removed vector omitted the deployer/name/nonce/salt inputs needed to reproduce it. Corrects the CF-4 listing vector to use an unencoded URL-safe `listingId`, matching the schema and fixed-segment table. Golden count: 204 → 203.
- **Revocation-marker lookup made executable** (§6.3.4–§6.3.7; #249) — fixes the post-#238 gap where LR-3 required readers to check a marker whose write-input-derived native address was not discoverable. The additive DACS-1 v0.3 `RevocationBinding` capability below supplies the missing resolution surface.

### Fixed — DACS-5

- **One-copy reputation lookup made fail-closed** (CORE §5 SR-2, §10.4.3 / §10.5.1 guard (iv), §A.2; #251) — closes the fetch-layer censorship path where read infrastructure could hide one of two divergent bundle copies and make the observed copy appear cleanly one-sided. SR-2 now distinguishes `present`, policy-qualified `absent`, and `indeterminate`; a binding claiming authoritative absence must declare its finalized-state, authentication, quorum independence/threshold, and freshness/consistency rules. DACS-5 applies one-sided attribution only after authoritative absence and excludes an unqualified one-copy jobId from every reputation metric. The current Demos Storage Program mapping declares no absence policy, so ordinary RPC/API `not found` fails closed as `indeterminate` until a policy is specified and conformance-tested. No signed wire field, universal quorum number, or cryptographic protocol is added; DACS-5 remains v0.3.

- **Shared-index `phaseSummary.kind` substitution classified** (§10.4.3 / §10.5.1 guard (ii); #250) — closes the unrouted case where two copies have identical phase-index sets and matching per-entry `outcome`/`errorClass`, but name different phase kinds at the same index. **Ruling: a shared-index `kind` mismatch IS a canonical divergence**, because the copies contradict about which protocol phase ran; it is not advisory skew. The §10.5.1 predicate mirrors the §10.4.3 definition so a reputation deriver excludes the disputed jobId from every metric rather than preferring either party's copy. One candidate vector pins the same-index substitution → divergent/excluded result. This correction does not change the DACS-5 version.

- **Divergent-bundle reputation policy aligned** (§10.4.3(d) / §10.5.1 guard (ii); #230) — removes the §10.4.3 example that allowed trusting each party's own divergent copy for its reputation while §10.5.1 required excluding the same jobId from all metrics. **Ruling: DACS-5 `ReputationDerivation` always excludes a canonically divergent jobId from every metric and MUST NOT select either copy for party-specific reputation.** Consumers may still apply an out-of-band dispute-handling policy to the independently signed evidence, but that result is not a conforming DACS-5 derivation. The existing golden divergence/exclusion cases are linked explicitly so consumer classification (`divergent`) and derivation disposition (exclude from numerator and denominator) assert one outcome. This correction does not itself change the DACS-5 version.

- **`phaseSummary` entry-presence divergence pinned** (§10.4.3 / §10.5.1 guard (ii); #219) — the "canonically diverge" definition covered a shared entry's `outcome`/`errorClass` but was silent on entry **presence**, and the two readings both existed live (dacs-sdk's deriver treated a length mismatch as divergence; its consistency classifier skipped one-sided entries — same jobId, opposite verdicts). **Ruling: an entry present in one copy and absent in the other IS a divergence** — the entry set is a normative input, a copy asserting a phase the other denies is a contradiction about which phases ran, and omission-tolerance would let a fabricated appended phase entry escape entry-wise comparison. Absolute rule — the draft terminal-phase carve-out was dropped on review (a terminal asymmetry is still a contradiction in the normative record). The golden conformance pair now pins presence-mismatch → divergent and advisory-skew → unified. Both reviewer passes; the agreement-artifact consumer capability below subsequently advances the current DACS-5 draft to v0.3.
- **Bundle logical→native resolution contract + absolute fault attribution — `BundleBinding` (BB-1..BB-8) and the `FaultAttestationBundle` type** (§10.4.1, §10.4.2, §10.4.3, §10.5.1, §10.8, §6.3.5, §6.3.6, §A.2, CORE §B.7, §14.5; #242, #251, PR #248) — corrects a defect in the `next` baseline (d289af1, pre-#248): §10.4.3 directed consumers to query `stor-{sha256(jobId + "-bundle-" + role)}` at both sides, but on Demos that 64-hex value is not the native address — the StorageProgram derivation folds in deployer/name/nonce/salt (§6.3.4) and is not recomputable from it — and no normative object, publisher rule, or discovery operation supplied the mapping, so the two-sided lookup was unexecutable (the same defect class as the v0.1 listing-address fix). The role address is now classified **logical**, and a signed **`BundleBinding`** object (`dacs-bundle-binding:v1:` separator, §B.7) supplies the contract: per-anchorer publication (BB-1) onto a consumer-reachable surface (BB-2; the §6.3.5 well-known block gains an optional `bundleBindings` index and the §6.3.6 catalog a `GET /api/dacs/bundles/{jobId}` endpoint); self-authenticating carriage (BB-3); signature/signer verification (BB-4); an explicit reject-on-any BB-5 resolution sequence over the signed tuple (`bindingVersion` supported, `jobId`/`role` equal the requested pair, `logicalAddress` equals the derivation from the signed pair, fetched bundle `jobId` equals the binding's, `bundleContentHash` equals the byte-exact §10.4.1 `attestation_bundle_hash`, signer holds `role`); canonical-inequality multiplicity — equal copies collapse, a fully-signed copy takes precedence over lesser-signed divergents, equal-standing divergence voids the side, with a deterministic N=8 fetch bound in ascending `bundleContentHash` order, fail-closed on exceed (BB-6); fail-closed never-recompute resolution (BB-7); and suppression diligence before one-sided classification (BB-8). Round 2 additionally closes **cross-role rebinding** with a distinct **`FaultAttestationBundle`** type (its own `faultBundleVersion` discriminator and `dacs-fault-bundle:v1:` §B.7 domain) carrying an absolute, hashed `faultedParty` with a permissible-set rule keyed on `(outcome, anchoredByRole)` (`failed-perm`/`aborted-by-self` → anchorer; `failed-counterparty`/`aborted-by-other` → any non-anchorer party role, exactly the counterparty in a two-party session; `completed`/`failed-substrate` → none) and a same-`faultedParty` invariant across the perspective pair. A consumer MUST reject a `FaultAttestationBundle` copy whose `faultedParty` is outside that permissible set, so re-anchoring a single-signed abort under the counterparty's role no longer silently reverses blame; the §10.5.1 deriver reads fault from `faultedParty == role_of_party` rather than the role-relative `outcome`, and the §10.4.3 "canonically diverge" definition is reconciled to read a `FaultAttestationBundle` pair on `faultedParty` + outcome class, not the role-relative spelling. The legacy `AttestationBundle` (`bundleVersion "1"`) carries no `faultedParty`, keeps the disclosed role-relative residual, and is never rewritten. Supersedes the earlier bundleVersion "2" design: adding a required, action-bearing field under a new enum value of an existing artifact is a breaking change per CORE §11.1.2 and cannot ship as a minor; the same fault-attribution model now ships as the distinct FaultAttestationBundle type with its own discriminator and §B.7 domain, leaving the legacy AttestationBundle untouched by construction. Executable coverage ships in `conformance/vectors/security/bundle-binding-v0.1.json` (9 candidate-tier vectors with genuine ed25519 signatures — valid resolution, invalid binding signature, tuple mismatch, content-hash mismatch, cross-role rebinding, missing binding, full-signature precedence, equal-standing void, N=8 limit; cross-run per CROSS-RUN.md pending a second impl). The surface-relative suppression residual (a binding suppressed from every surface a consumer reaches is indistinguishable from a never-anchored copy — the §10.5.3 completeness residual) is **narrowed by BB-2/BB-8 on the surfaces a consumer consults but not closed**, and remains tracked in **#251**; adjudication of a fabricated one-sided abort is a DACS-X concern. This corrects a defect but adds normative surface (two signed artifact types, eight rules, two separators, two discovery-surface extensions, and the fault-attribution model); per the steward call it is recorded under the already-unreleased DACS-1/DACS-5 v0.3 with no additional version bump. The #242 job-level binding manifest and Directory routes, and pathos-dacs-ref's `programName`-free resolver carrying `logical_address`, informed the design; neither implements `BundleBinding`.

### Fixed — DACS-4

- **Canonical x402 receipt hash — X402-1..X402-4** (§9.5.7; #246) — pins the existing `paymentReceiptHash` to the lower-case SHA-256 of RFC 8785 JCS over the complete decoded successful x402 `SettlementResponse`, after the recursive CORE CF-1 NFC pre-pass. Version `"1"` selects `X-PAYMENT-RESPONSE`; version `"2"` selects `PAYMENT-RESPONSE`. Property order, whitespace, escaping, base64 spelling, and canonically equivalent Unicode no longer change the DACS hash, while extensions and unrecognised receipt members remain committed. Raw header text, decoded non-canonical JSON, an on-chain receipt, and `settlementTxHash` alone are not valid preimages. Receipt transaction/network values must match the separately recorded settlement reference. Adds candidate normalization, preservation, malformed-response, placeholder-hash, and mismatch vectors without changing an artifact field or the DACS-4 v0.3 version.

### Added — DACS-3 v0.3 / DACS-4 v0.3 / DACS-5 v0.3

- **Byte-exact SB-3 EIP-3009 nonce binding** (§9.5.7/§9.5.8, CORE §B.7; #241) — replaces the open `H(jobId || phaseIndex || …)` placeholder with the third-party-recomputable `SHA-256(UTF8("dacs-sb3:v1:") || UTF8(NFC(jobId)) || ":" || ASCII(decimal(phaseIndex)))` construction. The phase index is minimal unsigned base-10; the result is the raw EIP-3009 `bytes32` and has a canonical lower-case `0x` text form. Handlers MUST use the derived nonce rather than a random/provider-generated value; verifiers recover `phaseIndex` from the SB-1 evidence anchor and compare the decoded bytes, treating a mismatch as rejection and malformed encoding as `error`. Retry handling reconciles an already-used nonce to the same completed transfer or fails closed — it never generates a second nonce/payment for the phase. Registers `dacs-sb3:v1:` as a non-signature hash-domain tag and adds byte-exact candidate vectors. This pins an existing SB-3 requirement; it does not change facilitator settlement semantics or add an evidence field.
- **Payee-destination binding — PB-1..PB-3** (§9.5.1, §8.5, §8.6, CORE §11.1.2/§11.2.5; #211, compatibility correction #228) — closes the payee-substitution surface without changing the semantics of the legacy `AgreementDocument`. The initial optional-`terms.payoutBindings` design was not minor-safe: a DACS-4 v0.2 payer could ignore the field and still pay the runtime destination. The corrected design adds the structurally distinct `PayeeBoundAgreementDocument`, required `payeeBoundAgreementVersion: "1"`, signing domain `dacs-payee-bound-agreement:v1:`, and `commit-payee-bound-agreement` phase. The legacy artifact retains `agreementVersion: "1"`, its existing signing domain, and pre-PB settlement behaviour. An old reader rejects the new artifact because the required legacy discriminator is absent (and rejects the new listing phase); it cannot silently ignore PB and pay. A current reader recognises both and applies PB only to the new type. **PB-1** now requires exactly one signed payout binding per pay-phase `(railId, phaseIndex)` tuple; missing/duplicate/wrong/extra entries fail before payment. **PB-2** retains the strongest-applicable intrinsic / control-proven `cci-xm:` / agreement-signed ladder. **PB-3** retains the no-downgrade rule, and the no-satisfiable-tier path is `MUST refuse` (#229). The conformance plan requires the cross-minor accept/reject case and PB rule row (#231); 28 candidate vectors now use the new artifact and include legacy-reader structural refusal.
- **`pay-ap2` attested provider-receipt verification + session binding** (§9.5.6, §9.5.8, §9.3, §9.13; #221) — closes the bare-provider-string gap on the fiat rail. A success-outcome `pay-ap2` record now MUST verify the provider receipt via an **SR-3 attested fetch** of the provider's payment-status endpoint (AP2-2 — the same consensus-backed-proxy machinery as the DACS-2 `lei` recipe), recorded in the new optional `ap2` txRef field `receiptAttestation` (REQUIRED on success). The handler MUST bind the session into the provider-side payment metadata at creation — pinned keys `dacs_job_id` (MUST) + `dacs_agreement_hash` (SHOULD) — registered as the **SB-3 binding for `pay-ap2`** (AP2-1), so a single attestation binds payment → session → agreed terms. The provider credential disclosed for the attested fetch MUST be read-only payment-status scope (AP2-3; it transits the SR-3 relay by construction — new §9.13 threat row). `provider-receipt` finality is pinned to **capture, not irreversibility** (AP2-4); post-capture reversal (chargeback) semantics are the #220 design item. Retry safety (AP2-5/AP2-6): because submission precedes verification, a `transient` retry MUST re-check provider status for the existing `providerRef` before minting a new mandate (AP2-5), and the submission MUST carry a deterministic `(jobId, phaseIndex)`-derived provider idempotency key so a retry after an unobserved receipt settles the same charge, not a second (AP2-6) — closing the double-charge window end-to-end. Stale §9.13 mandate-replay row updated (the SB-3-equivalent binding no longer awaits reference-backing). Mechanics validated end-to-end by the standalone fiat PoC (Stripe test mode + DAHR on Demos testnet, 2026-07). Reference-backing (live path + §14 vectors) remains open on the roadmap. Bumps **DACS-4 to v0.3**.
- **Metered (usage-based) pricing — MTR-1..5** (§8.5.2, §8.5, §8.9 PS-3, §6.3.6, §12.4; DACS-4 `PricingSpec`; #223) — adds a fourth `PricingSpec` variant `metered` (`unitPrice` + `unit` + optional `minTotal`) for per-unit billing. The quantity is carried first-class in the agreement as `terms.meteredQuantity` (`{quantity, unit}`, under every signature + the agreement hash), and commit-agreement recomputes the authoritative total: **MTR-4** — `terms.price` MUST equal `max(minTotal ?? 0, unitPrice.amount × quantity)` in CD-1, where `quantity` is a canonical unsigned-decimal integer count of whole units (raw measurement rounded **up**, so two implementations derive the same quantity); the product is exact (decimal × integer). MTR-4 binds the price to the *declared* quantity — the declared-to-actual binding is the buyer's computation co-signed by the seller, dispute-visible, not a measurement proof. DACS-1 schema conformance makes a pre-metered reader refuse the new closed-union variant; **MTR-5** independently requires a transacting reader to reject any unrecognized `PricingSpec.kind` at commit-agreement with a recorded `unrecognized-pricing-kind` reason. A **catalog/rendering** consumer (§6.3.6) MAY instead skip-render an unrecognized kind — one value, two consumer classes, opposite verdicts. PS-3 extended: `metered` pairs with `negotiate-fixed-price` or `negotiate-rfq`. Design #223; sign-offs: cX3po + xm33. Twenty-two candidate vectors now cover every MTR rule, including ceil derivation, exact recomputation, all forbidden quantity spellings, currency/unit mismatches, and unknown-kind refusal.


### Added — DACS-1 v0.3 / DACS-3 v0.3

- **Revocation-marker discovery binding — RB-1..RB-6** (§6.3.4–§6.3.7, §14.1; #249) — formalises `RevocationMarker` and adds `RevocationBinding`: a revoked index/catalog record is retained with its marker's logical address, native anchor, and exact unsigned canonical hash. The binding remains discovery-only while readers verify the fetched marker's hash, existing `dacs-revocation:v1:` signature, signer, and listing tuple. Missing/inconsistent bindings, failed fetches, stale records, and integrity failures are `indeterminate` and refuse new sessions rather than becoming a clean marker-absent result. Generic transport-view censorship/read quorum remains tracked separately in #251. Fourteen candidate security scenarios cover opaque and convention-named resolution, poisoned bindings, invalid markers, missing tombstones, stale/unreachable discovery, successful absence, and multi-surface precedence.
- **Sealed-envelope procurement role binding** (§6.3.4, §8.4.3, §8.5.2; #210) — adds a minor-safe `negotiate-sealed-envelope-procurement` phase kind for procurement listings, while existing `negotiate-sealed-envelope` remains demand/default with no mode discriminator. `demand` preserves the status quo: winning bidder = agreement `buyer`, listing publisher = `seller`. `procurement` flips the role assignment: listing publisher = agreement `buyer`, winning bidder = `seller`. DACS-1 keeps the wire-compatible `seller` / `buyerRequirement` listing fields but defines their procurement interpretation: `seller` is the listing publisher/signer, and `buyerRequirement` gates bidders / suppliers. In both modes, `bid.price` is the amount payable by the agreement `buyer` to the agreement `seller`, so money direction is determined by agreement roles rather than by who ran the auction. Commit-agreement now validates role direction against the pinned listing's phase kind and rejects role-inverted agreements before Settle; missing / unresolvable / malformed procurement `auctionMode` is rejected with an `unresolvable-auctionMode` reason and MUST NOT be coerced to demand. Adds SE-8, a §12.4 threat row, and eight golden negotiate vectors, including the old-bidder demand-shape-against-procurement rejection, the procurement missing-listing-publisher-signature rejection, the procurement-phase missing-mode rejection, and the unresolvable-mode arm. Additive/minor-safe because old readers reject the new procurement phase kind during listing validation instead of silently treating procurement as demand.

## [0.3] — 2026-07-03

Wave-2 capability release on top of v0.2: regulated-context cost disclosure (DACS-3's first capability bump) and private / access-controlled deliverables, plus a cross-impl defect clarification and the composition-boundary precision pass. **Version note:** the release tag is the coordinated-cut counter, decoupled from per-stage versions — per-stage versions under this tag: **DACS-1..5 all at v0.2** (DACS-3's first capability bump lands in this release).

### Added — DACS-3 v0.2

- **`FeeSchedule` disclosure** (§8.5.3, §8.5; #186) — an optional, signed pre-commit `terms.feeSchedule` on the AgreementDocument for regulated-context cost transparency (EU MiCA / US MSB). New `FeeSchedule` / `FeeItem` types: itemised fees (`kind`, `collector`, `fixed` | `rateBps`), `priceBasis` (inclusive/exclusive), split `oneOffTotal` / `recurringTotal`, optional `recurrence` (subscription cadence) + `minimumTermSeconds` + `earlyTerminationFee`. **Disclosure-only / non-gating** — does not alter `terms.price` or gate Settle (FS-1..FS-5); `recurrence` discloses a committed ongoing cost, never a charge trigger (recurring *settlement* stays the streaming/subscription roadmap item); `earlyTerminationFee` is a disclosure shape only, with fee-bearing-cancellation semantics deferred to a dedicated §10.3.1 (ST-10 `with-fee`) pass. Bumps **DACS-3 to v0.2** (its first capability bump). Additive, minor-safe under §11.1.2 (optional field, SIG-5 preserve-unknown, never gates Settle). Design + sign-off: cX3po + xm33.
- **`AgreementParty.encryptionKey`** (§8.5; #190) — optional party encryption public key, binding which key an `encrypt-to-buyer` private deliverable is sealed to (DACS-4 §9.6.1, DV-3); distinct from the signing key. Additive, minor-safe.

### Added — DACS-4 v0.2

- **Disclosed-fee reconciliation** (§9.7.2; #186) — an **informational, non-gating** verifier rule reconciling a DACS-3 `feeSchedule` against actual settlement. Scoped to the **`network`-kind item only** (against `SettlementEvidence.paymentFee`); off-chain fees (platform/processing/spread/subscription) stay disclosure-only (FR-1). Pins the canonical **`rateBps → amount`** computation — `price × rateBps ÷ 10000`, CD-1 canonical decimal, **half-up to the settlement asset's `decimals`** — so two impls derive identical amounts (FR-2); expected total computed via `priceBasis` (FR-3). Do-not-collapse verdict (FR-4): *reconciles* (network within self-declared `toleranceBps`, default exact; `fixed`/`rateBps` exact after rounding) / *diverged* (provenanced flag carrying the signed delta — under- vs over-disclosure) / *indeterminate* (actual fee or agreement unresolvable — never `diverged`). Observes, does not reallocate (incidence is set by `priceBasis` + rail fund flow). Conformance-vector shapes (recurrence-non-gating + network-fee reconciliation incl. the `indeterminate` arm) landed as the candidate set `feeschedule-reconciliation-v0.1` (#206, cX3po).
- **Private deliverables + entitlement credential handover** (§9.3, §9.6.1, §9.6.2; #190, folds #61) — an optional `accessModel` (`public` | `buyer-only` | `encrypt-to-buyer`) on the `storage-program` deliverable, surfacing the shipped Demos storage-program ACL (`buyer-only`) and the native post-quantum `UnifiedCrypto` `ml-kem-aes` envelope (`encrypt-to-buyer`); content is gated while the **cleartext content-hash stays public** so settlement evidence is comparable across modes. **DV-1..DV-6**: content-hash invariant (cleartext, byte-identical across modes); access-mode fidelity (declared non-`public` delivered `public` → `indeterminate`, never silent pass); agreement-bound buyer ACL/key (via DACS-3 `AgreementParty.encryptionKey`); ACL-mutation auditability (SHOULD for deliverables, MUST for credentials); and for the folded-in #61 `EntitlementRecord.credentialRef`, the three-gate split (delivered / valid / readable — `SettlementEvidence` binds only *delivered*) + the readability do-not-collapse verdict (readable / lapsed / ACL-dropped / unresolvable→`indeterminate`; an ACL-drop is channel-provable unreadability, not credential invalidation — full revocation also needs endpoint-side rotation). Pure spec — both modes substrate-available today (demosdk 4.0). Design + sign-off: cX3po + xm33.

### Fixed — DACS-5

- **`phaseSummary[].attestationRef` optionality clarified** (§10.4 / §10.4.3; #204) — the field was declared optional (`attestationRef?`) but two conformance validators diverged (`dacs-sdk` **required** it; `pathos-dacs-ref` treated it **optional**), surfaced during the dacs-sdk PR #2 decision-convergence run. **Ruling: OPTIONAL** — the authoritative attestation set is the top-level `vetRecords[]` / `settlementEvidence[]`; a validator MUST NOT reject a bundle solely for omitting a per-phase `attestationRef`. A phase producing a durable attestation SHOULD carry it to disambiguate per-phase evidence in multi-phase pipelines. Minor-safe clarification — a *required* per-phase ref would have been a major bump under §11.1.2, disproportionate for a back-pointer already covered by the top-level arrays. DACS-5 stays v0.2.

### Editorial

- **Composition-boundary precision pass** (CORE §C.1, THREAT-MODEL §12.4 rows, DACS-4 §9.5, DACS-5 §10.10; #194 via #205) — the "inherited from AP2/x402" framing replaced with evidence / local-verifier language: upstream standards contribute *evidence*; DACS binds it to the action and records its **own** verifier act (unresolvable reference → `indeterminate`/`error`, never a borrowed `pass`). New **§C.1 contributes-vs-must-bind matrix** (AP2 · x402 · ERC-8004 · A2A) with per-standard v0.1 status; AP2 marked *specified, not yet reference-backed*; ERC-8004 scoped to identity-anchor-only (reputation integrity is DACS's own). Non-normative. Audit + PR: cX3po; framing: mj-deving.
- **Candidate security-vector suite + conformance tooling** — the candidate tier grew to **9 sets / 154 vectors** (#193 §8.5.2 + §7.3.2 · #200 §7.12 + §9.4.4 + §8.4.3 · #203 §8.3.3/CH-6 · #206 FS/FR · #207 DV), all cX3po/pathos-dacs-ref-authored, MANIFEST-untouched. Steward tooling: per-set CI validation (`validate_security_vectors.py`), a generated set index, and the **CROSS-RUN.md** candidate→golden protocol + `diff_vector_runs.py`. Non-normative.

## [0.2] — 2026-06-26

Defect-triage and follow-on hardening on top of v0.1 (steward-merged via the issue triage of the cX3po review wave and the xm33 / DACS-X contributions).

### Added — DACS-1 v0.2

- **Verified-vs-controlled claim gate** (§6.3.2 step (6), Verified-presentedBy; #170) — closes a v0.1 defect: the verification-resolution gate keyed only on `decision == pass`, so a **validity-only** verification (a bare-registry `lei` GLEIF lookup) was indistinguishable from a control-establishing one, letting any key present a *public* LEI as a controlled identity. New resolution **step (6)**: a claim may serve as a **controlled** claim (`presentedBy` / reputation key) only if the presenter **proved control** — a DACS-1 property: the bundle presentation signature (`key:`), the anchored address-key linkage (`cci-xm:`), or a credential **holder-binding** proof (VC / vLEI). An existence-only `pass` is **valid-but-uncontrolled** (satisfies a required claim + supporting context, but never `presentedBy`/reputation). Control follows the **proof, not the storage** (CCI residency confers nothing). Confirmed real by three impls (pathos-dacs-ref, dacs-verify, DNO). Bumps **DACS-1 to v0.2**. *(The DACS-1/DACS-2 control-**boundary refactor** — physically re-homing the cross-method binding step — is parked as a follow-on; this fix lands the gate without it.)*
- **`cancellationPolicy: "pre-commit"` honoured** (§6 listing-terms note; #92) — the v0.1 `cancellationPolicy` field was informational-only (no enforced representation). The §6 note is rewritten: a **signed** listing advertising `pre-commit` now grants a reputation-neutral cancellation right, verified and scored in DACS-5 §10.3.1 ST-10 / §10.5.1. `none` and the reserved `with-fee` confer no neutrality. Additive note change, no schema change. DACS-1 already at v0.2.

### Added — DACS-2 v0.2

- **Existence/validity, never control** (§7.3.2 area; #170) — a DACS-2 `VerifyResult` establishes that the identifier is real/valid at its authority and a `decision: "pass"` MUST NOT be read as proof the presenter *controls* it; control is determined in DACS-1 §6.3.2 step (6). A bare-registry method (`consensus-backed-proxy`) can only ever establish existence. No schema change. Bumps **DACS-2 to v0.2**.
- **`lei` registration-status → decision mapping** (§7.4.1 worked example; #146) — the v0.1 `lei` recipe decided on LEI **presence** only, so a LAPSED/RETIRED registration still returned `pass`. Now it decides on GLEIF **`registration.status`** (primary; `entity.status` is diagnostic and MUST NOT override): `ISSUED → pass`; `LAPSED`/`RETIRED`/`PENDING_TRANSFER`/`PENDING_ARCHIVAL → indeterminate` (real but not-current — do-not-collapse); `ANNULLED`/`DUPLICATE`/`MERGED`/`CANCELLED` or 404 `→ fail`; reachable-but-error `→ error`. Encoded via the `successJsonPath` ISSUED-filter + `indeterminateOn` predicates (PSP-2). `RETIRED → indeterminate` is a steward decision (#152). `resolvedEntity` = NFC `entity.legalName.name`. Source-grounded against LEI-CDF 3.1; live-proven by two independent impls (PATH-OS + DNO) converging on ISSUED/LAPSED/RETIRED. Closes the last Wave-1 item.

### Added — DACS-4 v0.2

- **Session-bound settlement evidence — rules SB-1..SB-3** (§9.5.8; §9.5.7, §10.5.1, §14.4) — binds a single-chain / provider settlement tx to its session, the equivalent of HTLC-5's jobId-derived preimage for the rails that lacked one. **SB-1**: a `SettlementEvidence`'s `paymentTxRefs` bind to `(jobId, phaseIndex)`, keyed by consumers as `(settlement-tx-id, jobId, phaseIndex)`. **SB-2**: a consumer aggregating evidence across sessions (incl. the §10.5.1 reputation reconciliation) MUST NOT count one `settlement-tx-id` under more than one `(jobId, phaseIndex)` — closing cross-session double-count, rail-agnostic, no on-chain change. **SB-3**: a rail MAY bind `jobId` on-chain; for `pay-x402` the `jobId` MUST be in the EIP-3009/Permit2 authorization and match `evidence.jobId` — closing coincidental-citation (the authorization MAY be an ERC-1271 contract signature for ERC-4337 smart-account payers — absorbs #147). Bumps **DACS-4 to v0.2** (per-stage versioning). Design note #159; closes the "session-bound settlement evidence" roadmap item. Additive + behavioural — no artifact schema change.

- **`pay-solana-spl` payer-funded ATA-rent** (§9.5.3, §14.4) — v0.1 left unspecified who funds the rent-exempt reserve when a payee's SPL associated token account (ATA) must be created, and whether the handler creates it. Now: a `createPayeeAtaIfMissing` rail parameter (default `false`) gates creation; the **rent-exempt reserve is funded by the payer** (included in the payer's required-balance preflight); and a payer who cannot cover the reserve fails `permanent`. Procedure clarification — no schema change (the param rides on the open `rail.parameters` map).

- **`pay-dem` — native-DEM settlement rail** (§9.5.9; railType `demos-native`, railId `demos-native:DEM`; #168) — settle the agreed price in DEM via a native Demos transfer, closing the gap that **no v0.1 rail settled in the substrate's own token** (all were foreign chains / cross-chain primitives / providers). **Single-payee in v0.2**; multi-payee fee-split (#85) is a follow-on. Adds the **`bft-final` finality model** (BFT inclusion is final — no block-depth / commitment tier, per @xm33), a `blockNumber` on the `demos` `ChainTxRef`, and the `demos:{txHash}` SB-1 binding form. Trust = Demos consensus itself ("operator is the substrate"), no external trust. Adds the additive type arms the rail needs: `AssetSpec` `native-dem` (symbol `DEM`, 9 decimals), `NetworkSpec` `demos`, `PaymentPhaseType` + DACS-1 `PhaseType` `pay-dem`, and the PC-5 `native-dem → rail.asset.symbol` mapping (review catch by mj-deving, #184 — the §9.5.9 prose referenced these before the closed types carried them). Design endorsed by cX3po + xm33 (#168). DACS-4 already at v0.2.
- **Liquidity-tank recovery-pending evidence** (§9.5.5; ST-8 §10.3.1; #25) — a locked-but-not-yet-released tank lock whose substrate recovery path is still open is now **recovery-pending**, machine-distinguishable from a terminal loss: the orchestrator anchors an interim `tank-locked-unreleased` record (`liquidity-tank` txRef with `lockTxHash` + a new `recoveryDeadline`, no `releaseTxHash`) and routes to the non-terminal `settle-asymmetric` state. **ST-8 generalised** from HTLC-9-only to two asymmetric-open cases: the release landing within `recoveryDeadline` resolves to success; the deadline expiring resolves to reputation-neutral `failed-substrate` (vs HTLC-9's `failed-counterparty` — no party is at fault for a substrate-recoverable lock). Replaces routing a recoverable lock through the short ST-7 substrate pause (which would finalise it prematurely). Closes the v0.1 success-only-evidence gap. DACS-4 already at v0.2; DACS-5 ST-8 touched.
### Added — DACS-5 v0.2

- **Blame-weighted completion + per-currency transaction count** (§10.5.1, §10.5, §14.5) — two additive `ReputationDerivation.metrics` fields. **`counterpartyAdjustedCompletionRate`** is `completionRate` with counterparty-caused outcomes (`failed-counterparty`, `aborted-by-other`) *also* dropped from the denominator, so a party is not penalised for a counterparty's abort/failure — closing the griefing residual v0.1's `completionRate` leaves (a counterparty repeatedly opening-and-aborting could depress an honest party's rate). **`transactionCountByCurrency`** records the per-currency count of completed sessions composing `observedTransactionalVolume`, strengthening the §10.11 anti-collusion "few large vs many small" heuristic. Both additive — `completionRate` and `observedTransactionalVolume` unchanged, no schema migration. Bumps **DACS-5 to v0.2**. (Dropped as low-value: the FX-normalised volume aggregate and the ERC-8004 cross-claim hint.)
- **Session-deadline timeout — ST-9** (§10.3.1; #148) — gives a `*-pending` session a terminal exit when a counterparty goes silent (no party decision, no phase result, no substrate condition). **Reuses the existing `*-pending → aborted-by-other` transition** with a `timeout` reason marker — no new state or `outcome`. The timeout is attributed to the party who **owed the awaited action** (an objective state-machine fact, not motive inferred from silence), and the **deadline MUST be objective** (SR-2 anchor / agreed substrate time, never a party-supplied clock). Reputation unchanged: the silent party bears the standard `aborted-by-other` counterparty-fault mark (no cheaper escape than an honest decline); the waiter stays neutral. Complements the SN-4 nonce-lifetime backstop with a real state-machine terminal. Endorsed by cX3po + xm33. DACS-5 already at v0.2.
- **Policy-permitted pre-commit cancellation — ST-10** (§10.3.1, §10.4, §10.5.1; #92) — honours a listing's advertised `cancellationPolicy: "pre-commit"` as a **reputation-neutral** cancellation, closing the v0.1 gap where a contractually-permitted cancellation took the same fault as a bad-faith bail. Per the §11.1.2 additivity contract, this is done **without a new `outcome`/`SessionState` enum value** (which would force a major bump): a pre-commit withdrawal stays the existing `aborted-by-self` transition (ST-3) and carries a new **optional** `cancellation` marker (`claimedPolicy`); §10.5.1 reputation drops a *verified* marker from the fault denominator (neutral, like `failed-substrate`). An old reader ignores the marker and books the ordinary self-fault — the safe direction, never an unearned free pass. **Teeth** (cX3po): the verifier MUST resolve the bundle's `listingRef` to a listing whose `ListingSignature` verifies and read the binding policy from that **signed** listing, never the asserted `claimedPolicy`. **Trichotomy / do-not-collapse** (xm33): resolves-and-permits → neutral; resolves-and-forbids → ordinary `aborted-by-self` fault; cannot-resolve → **indeterminate** (excluded from all denominators, never neutral, never a fresh fault) — the same `pass`/`fail`/`indeterminate` stance as §7.5.1 / SB-3. Symmetric (no `-by-self`/`-by-other` split): §10.5.1 resolves the marker across **both** non-divergent copies and excludes an established cancellation from the fault denominator regardless of whether the scored-party outcome is `aborted-by-self` *or* `aborted-by-other`, so the non-cancelling party does not eat a counterparty-fault on a validly-cancelled session; a one-sided marker is not a §10.4.3 divergence and is resolved across copies, not treated as advisory (review catch by mj-deving + xm33, #184). The cancelled count stays observable (attribute, don't hide). `with-fee` (post-commit, #85) explicitly reserved/undefined. DACS-1 §6 `cancellationPolicy` note rewritten from informational-only to honoured `pre-commit`; DACS-1 + DACS-5 already at v0.2. Endorsed by cX3po + xm33.

### Added — versioning / forward-compatibility (CORE)

- **Minor-version additivity contract** (§11.1.2; §11.2.5; DACS-1 §6.3.4 step 2; #79, #94) — closes a latent inconsistency: §11.2.5 made cross-minor forward-compatibility load-bearing and §6.3.4 step 2 a normative gate, but `*Version`/`dacsVersion` are major-only, so the cross-minor guarantee was not executable from the artifact. Pins the contract that makes it executable: **a minor version MUST be additive and forward-readable** — only optional fields / new registry entries / new types; anything an older reader must *act on* (a new required field, a new value of a branched-on enum, or changed semantics) is a **major** bump. With this, both skew directions hold with **no per-artifact minor-version field**: later-reads-earlier (superset reader) and older-reads-newer (SIG-5 preserve-unknown + nothing act-requiring in a minor). The §6.3.4 step-2 `dacsVersion` gate is clarified as **major-only** and is correct/complete for minor skew; the proposed `producedUnder` field is **dropped** (superseded by the contract). Cross-*major* compatibility remains deferred.

### Editorial

- **Conformance-summary tables completed (§7.9, §9.10)** — the role→rule-range summary tables omitted families that carry MUSTs, so an implementer using a table as a checklist would silently miss obligations. DACS-2 §7.9: PSP-1..5 and WN-1..4 added to the Verifier row; WN-5/6 and GOV-3 to the VerifyResult-consumer row; GOV-2 and PA-1..3 to the steward row (renamed "availability & governance"); a ParserSpec cross-ref on the Recipe-author row. DACS-4 §9.10: new "Orchestrator (rail selection)" row carrying RAV-R1..R5; the consumer row's "amendment chain following" made an explicit AMEND-1..4 range. Also tightens the §7.5.1(a) retry summary, which read looser ("transient by default") than the governing VP-R1/VP-R3 (retry only when `retryClass == "transient"`; never in-session for `permanent`), and swaps one "DAHR-attested" type comment in DACS-3 to the substrate-neutral "SR-3-attested". No rule-text changes beyond the §7.5.1(a) alignment; rule families and their semantics are unchanged. Surfaced by the DACS-2/DACS-4 readability reviews; closes the last item of the 2026-06 defect pass.
- **JCS canonicalisation defined once (§B.2 template)** — the seven per-artifact "canonical form is RFC 8785 JCS … sha256 … domain-separated payload" recitations (bundle, listing, VerifyResult, channel envelope, agreement, settlement evidence, attestation bundle) collapse to one **Per-artifact canonical-form template** rule in CORE §B.2; each site now states only its artifact-specific facts (omitted field(s), exact domain separator, exceptions such as DACS-4's `supersedesEvidenceRef` inclusion and DACS-5's `anchoredByRole` omission). Adds an explicit disambiguation of the computed attestation-bundle hash vs `BundleParty.bundleHash`. No normative change intended; keyword census and all domain separators verified unchanged; adversarial review 0 critical (1 major fixed pre-commit). Size-neutral — the gain is a single drift-proof definition, not length.

- **CORE back matter moved to companion references** — §A (Demos production mapping), Chapter 12 (unified threat model), Chapter 13 (glossary), and Chapter 14 (conformance test plan) moved out of `spec/CORE.md` into `spec/DEMOS-MAPPING.md`, `spec/THREAT-MODEL.md`, `spec/GLOSSARY.md`, and `spec/CONFORMANCE-PLAN.md`. **Pure relocation — no text changed**: section/chapter numbering is retained so every existing citation (§A.x, §12.x, §13, §14.x) remains stable; CORE keeps pointer stubs under the original headings; the validator source list (`scripts/specsource.py`) includes the companions so the concatenated spec text the checkers see is unchanged. CORE drops from ~13.7k to ~7.0k words; the normative reading surface (CORE + five modules + Primer) from ~58.1k to ~51.4k (−11.5%).

- **DACS-4 §9.4–§9.5 two-voice readability rewrite (pilot)** — normative rule text separated from non-normative `Note` blocks per the new [SPEC-STYLE](SPEC-STYLE.md) conventions; rationale, roadmap items, and deployment metadata moved to Notes; dense paragraphs split into rule-per-bullet structure; worked PC-2 address example added. **No normative change intended**; verified by the SPEC-STYLE protocol (rule-ID census identical; keyword deltas limited to one documented RAV-R5 MUST/MUST-NOT split and one HTLC-7 REQUIRED-tag merge; all validators and tests green; independent adversarial review: 0 critical findings). Introduces `SPEC-STYLE.md` and `scripts/prose_lint.py`.
- **Full-spec SPEC-STYLE readability sweep (CORE + DACS-1..5)** — the pilot conventions applied to the whole spec: fused bold-led paragraph runs separated (rendered as walls of text before); dense normative paragraphs split rule-per-bullet; rationale/roadmap/history moved to `Note (non-normative)` blocks; run-on procedures numbered; per-kind parameter prose → table. **No normative change intended**; each module verified by the SPEC-STYLE protocol — rule-ID census identical per module, keyword censuses unchanged (one documented MUST-NOT explicitation in DACS-4 §9.6.2), all validators + tests green, and one independent adversarial cold review per module (0 critical findings across all six; majors/minors fixed before merge). prose-lint findings across the spec: ~491 → ~310.

### Added

- **Session-nonce provenance — rules SN-1..SN-4** (CORE §B.8, §6.3.2, §7.3.2, §14.6) — the session nonce is the anti-replay anchor for identity presentations (§6.6) and the DACS-2 holder-/attestation-binding checks (§7.3.2), but only its *match* check was specified — not where it comes from, so two conforming implementations could disagree on the value the whole replay defence rests on (finding D9, #133). New CORE §B.8 (a cross-cutting security primitive, peer to the §B.7 signature scheme) defines its provenance: the **verifier** generates it (SN-1), ≥128 bits from a CSPRNG, native `sessionNonce` as ≥32 lowercase-hex chars (SN-2), issued before presentation bound to the session `jobId` with transport out of scope (SN-3), single-use (SN-4). DACS-1 §6.3.2 keeps the field/conveyance and cites §B.8 for provenance; DACS-2's binding checks cite it too. Placed in CORE because both DACS-1 (presentation) and DACS-2 (verification) depend on it — removing the prior DACS-2 → §6.3.2 reach-in.
- **`SettlementFinalityRecord` on SettlementEvidence** (§9.7, PC-6, §14.4) — payment evidence now records *how firmly* a payment was confirmed (`model`: block-depth / commitment-level / provider-receipt / htlc-reveal / liquidity-tank, plus the `finalityBlocks` / `finalityCommitmentLevel` actually applied and `finalityObservedAt`), echoed from rail params so the record is self-describing; required on success-outcome payment evidence, absent on delivery evidence. Promotes a ROADMAP candidate into v0.1 (#109, pallytin88).
- **`PriceAnchor` on AgreementDocument.terms** (§8.5) — optional DAHR/SR-3-attested reference-price snapshot both parties sign against (CD-1 price, raw-byte attestation contentHash); informational, does not constrain `terms.price`, and verifiers MUST NOT reject when absent. Promotes a ROADMAP candidate into v0.1 (#110, pallytin88).
- **`ReputationHint` at discovery + category-scoped derivation** (§6.3.6, §10.5.4, §14.5) — an optional catalog-computed reputation snapshot on `ListingSummary` (non-authoritative, staleness-stamped, MUST-verify-against-bundles for high-stakes), and a `derive_category_scoped` algorithm that restricts the bundle set to the listing's `offering.category` prefix before applying §10.5.1, so reputation reflects the relevant service domain. Promotes two ROADMAP candidates into v0.1 (#111, pallytin88).
- **Canonical-form rule family CF-1..CF-4** (§7.2, §6.3.1, §6.3.4) — CF-1 global NFC pre-canonicalisation of all signed/hashed string values; CF-2 ClaimReference canonical byte form (scheme lowercased before hash/sign/compare, identifier NFC, parameters sorted + uppercase-hex percent-encoded); CF-3 ClaimReference canonical identity = (scheme, identifier) only, parameters excluded from reputation keying; CF-4 logical-address delimiter encoding so the `dacsN:` logical address is reversibly parseable. Closes #7, #42, #54.
- **SIG-5 preserve-unknown** signature verification + **`version_tag` = major-version** binding (§7.7), making §11.1.2 forward-readable shapes hold for signed artifacts. Conformance vectors for CF-1..CF-4 / CD-1 / SIG-5 added to §14.6. Closes #74.
- **Normative session-state transition table** (§10.3.1) — rules ST-1..ST-7 (forward-only, abort entry from any `*-pending` framed as a party's right, rate branch, rate-non-fatal with no `rate-failed` state, terminal-state set + `endedAt`, substrate pause/resume) plus a state→bundle-`outcome` mapping resolving the `transient`/`settlement-atomicity` buckets. Closes #41, #80, #81, #91, #93, #97.
- **`rfqInitiator` negotiate-rfq phase parameter** (§6, §8.4.2) — `'buyer' | 'seller'`, default `'buyer'`, defining who sends the first RFQ offer; the prose that called it a listing field is corrected. Closes #20.
- **`ClaimRequirement.recipeVersion` pin** (§6.3.3, §7.4.1) — per-claim exact DACS-2 recipe-version pin (symmetric with the rail-side `railVersion`), making the §7.4.1 "pinned recipe" branch reachable and restoring the mid-session-revision protection. `docs/flow-trace.md` reconciled. Closes #82.
- **Rating bounds — rules RT-1 / RT-2** (§10.6.1, §10.5.1, §10.8, §14.5) — producer MUST reject a RatingRecord whose `value` is not an integer in [1,5] or whose `freeText` exceeds 1000 chars (RT-1); deriver MUST exclude non-conforming records before aggregation (RT-2), closing the metric-poisoning gap where a self-signed `value: 1e9` would inflate the average. Closes #84.
- **Deterministic `identityTier` derivation — rules IT-1..IT-3** (§6.3.2.1, §14.1, xm33) — an OPTIONAL single-word identity-quality signal (`institutional` / `verified` / `self-declared`) derived deterministically from a bundle's verified-and-fresh claims; never trusted as self-reported (readers MUST recompute and ignore any self-asserted value), with institutional = the authority-issued scheme tier (lei/finra-crd/sam-uei/fedramp/cmmc/naics) so it cannot disagree with the §6.3.2 presentedBy ordering. Promotes a contributor RFC into v0.1. Closes #103.
- **Implementation-readiness tooling + conformance scaffold (non-normative)** (`conformance/`, `scripts/`, `tests/`, `.github/`, `docs/`, hackobi, #119) — a CI suite of drift-detecting validators (domain-separator registry, rule-ID index, spec tables, doc links, conformance-vector shape) with 35 unit tests, GitHub issue templates + a `validate.yml` workflow, a nested `conformance/` fixture+vector tree, and navigation docs (rule-ID index, glossary index, operational builder guide). Non-normative — the specification text remains authoritative; these are an aid for implementers and contributors. Standardises the `conformance/` layout on the nested scheme.
- **Advisory verification warnings — rules WN-1..WN-6** (§7.7, §14.2, xm33) — an OPTIONAL `warnings?: VerificationWarning[]` on `CompositeVerificationRecord` surfacing transient/retryable conditions (enumerated `WarningCode`: AUTHORITY_UNAVAILABLE / _RATE_LIMITED / DNS_RESOLUTION_FAILED / TLS_HANDSHAKE_FAILED / RESPONSE_MALFORMED / RETRY_EXHAUSTED) without moving the decision: warnings are strictly advisory, MUST NOT change `overallDecision` or the §7.7.1 aggregation, are preserved even on `pass`, and `suggestedRetryAfterMs` does not override the recipe `backoff`/`retryBudget`. Aligned to the §7.6.1 VP-R1 retry taxonomy. Promotes a contributor RFC into v0.1. Closes #102.

### Changed

- **`counterpartyDisputeRate` renamed to `counterpartyFaultRate`** (§10.5.1, `ReputationDerivation` schema) — the metric counts counterparty *faults* (`counterparty_fault_count / party_fault_denom`); disputes are a DACS-X concept that does not exist in v0.1, and the old name would collide with real dispute metrics when DACS-X ships. Values and formula unchanged — key rename only, applied pre-baseline while no implementation depends on the schema. Conformance vectors and fixtures renamed in step (incl. the `disputeIsZero` → `faultIsZero` assertion key); the reference implementation's `derive()` output key needs the matching rename. Surfaced as a Med finding in the PATH-OS baseline review (#118, cX3po).
- **Six regulatory CCI contexts deferred out of v0.1** (§A.1, §6.3.1, ROADMAP) — the planned Demos-native CCI contexts `lei`, `finra-crd`, `sam-uei`, `fedramp`, `naics`, `cmmc` move from "to add for v0.1" (🟡) to **deferred to a later version** (⚪, new legend entry). The scheme grammar, claim tiers, and DACS-2 verification recipes are substrate-independent and unchanged; until the contexts ship, regulatory credentials on Demos are carried via the `stor-cred:` extensibility surface (§6.3.1). Steward decision.

- **`RatingRecord.dimensions` declared opaque pass-through** (§10.6.1) — not interpreted or aggregated by DACS-5 derivation, no protocol semantics, unconstrained keys/ranges; consumers MUST NOT rely on it for conformance decisions. A canonical dimension namespace is a roadmap candidate. Closes #96.
- **`cancellationPolicy` declared informational-only in v0.1** (§6.3.4 listing terms) — no enforced `cancelled` state/outcome; advertised policies are not binding, reputation-neutral exits, and the §10.3.1 abort semantics govern. A first-class `cancelled` outcome is a roadmap candidate. Closes #92.
- **Settlement over-promises reconciled to v0.1 scope** (§9.9, §9.5.5) — patterns the spec named but the v0.1 model cannot express are now scoped out and roadmapped, removing four internal contradictions: (i) **escrow-with-delivery-gate** — the v0.1 atomic-swap `pay-cross-chain-htlc` handler has no mid-lock suspension point, so "deliver between lock and reveal" is reserved for a future job-escrow rail (ERC-8183) (#8); (ii) **streamed entitlement** — reframed as a sequence of independent sessions (one pipeline = one jobId), with continuous-flow/subscription settlement out of scope per §11.2.4 (#64); (iii) **PIPE-5 fee-split** — narrowed so repeated pay-* phases settle the same agreement price; distinct-amount/multi-payee fee-split is unrepresentable in v0.1's single-amount contract and is roadmapped (#85); (iv) **liquidity-tank recovery-pending evidence** — v0.1 tank evidence is success-only; a recovery-pending marker distinguishing a recoverable lock from terminal loss is roadmapped (#25). Roadmap rows added/extended for all four. Closes #8, #25, #64, #85.

### Removed

- **`membership-change` reserved out of v0.1** (§8.3.1 CH-1, §8.3.3 `ChannelMessage.type`) — channel membership is fixed for the channel's lifetime in v0.1; the undefined `membership-change` type and `admissionPolicy` are removed from the closed enum and reserved for a future version (dynamic membership is a roadmap candidate). Closes #21.

### Fixed

- **`validate_artifact_shapes.py` extended: scans `examples/` and checks `ClaimReference` string-form.** The shape checker now also covers the single-artifact `conformance/vectors/examples/*.json` wrappers (previously only the top-level lifecycle vectors), and — derived from the spec types, resolved **per type with nested descent** — asserts that every `ClaimReference`-typed field is serialised as a string (`"scheme:identifier"`), not an object. This catches a class the field-set check alone could not: a verifier emitting `party: {scheme, identifier}` produces a `bundleHash`/signature over a non-spec serialisation (surfaced reviewing #145's regenerated bundle). Per-type resolution avoids a field-name collision a global check would hit (`AttestationBundle.parties` is `BundleParty[]`; `CommitmentRecord.parties` is `ClaimReference[]`). Stdlib-only, three new unit tests.
- **`AgreementDocument.terms.rail` reconciled with zero-pay pipelines (§8.4.1, §8.5.2).** The field was required, but PIPE-1 (§9.5) allows a pipeline with **no `pay-*` phase** (intake-only / settled-out-of-band: free services gated by reputation, RFP intake, sealed-bid procurements settled out-of-band) — so a payment-free agreement was forced to carry a rail it has no use for. `terms.rail` is now optional (`rail?`), and §8.5.2 rule 3 makes it **conditionally required**: present iff the pipeline contains a `pay-*` phase (and then it MUST appear in `listing.acceptedRails`), absent for a zero-pay pipeline. The §8.4.1 fixed-price construction step notes the omission. Loosens a constraint (backwards-compatible); the rail-validation rule's keyword shape changes accordingly. Surfaced by @mj-deving's #122, rebased onto the module layout — its `windowingBasis` change had already landed via the determinism receipt, and `fixedPriceFallback` is already specified (§8.8).
- **Conformance vector artifacts now CI-checked against their spec type shape (D2 guard).** New `scripts/validate_artifact_shapes.py` derives each artifact's field set directly from the spec's `type X = { … }` blocks (single source of truth — no second schema encoding) and checks every lifecycle-vector artifact: all required fields present, no unknown fields. This closes the gap that let the two `dacs-v0.1-*` lifecycle vectors carry **pre-v0.1 artifact shapes** with green CI — the existing checks validated only the wrapper + content hash, never the field set (finding D2, #133). The two stale vectors are **quarantined** (skipped with a loud, documented notice + a `⚠️ stale` banner in `conformance/vectors/README.md`) pending regeneration from a reference verifier, so the check is *not* fabricated-clean and the gap is disclosed, not hidden; it guards every other and every future vector meanwhile. Stdlib-only, wired into `validate.yml`, unit-tested. The current verifier-emitted suite remains `golden.json` + `MANIFEST.json` (186 cases).
- **Conformance §-references now CI-checked against real spec headings.** New `scripts/validate_spec_refs.py` (cX3po, #137) resolves every `§` pointer carried by the conformance corpus — MANIFEST `spec` fields and vector `specRefs` — against actual spec headings, so a moved/renumbered/deleted section can no longer leave a vector citing a dead pointer with CI green. Resolution is exact-or-ancestor-of-a-heading (`§10.4` is valid when `§10.4.3` exists; `§A.99` is rejected). Stdlib-only. Wired into `validate.yml` and covered by a unit test (the CI step and test added on merge, since the author's token lacked `workflow` scope). Complements `validate_rule_id_index.py` (rule-family pointers) and `validate-docs.py` (markdown links); together they close the "CI checks structure, not pointer resolution" gap behind findings D8/D2 (#133, cX3po).
- **Rule-ID index section pointers corrected, and a validator added so they can't drift again.** 12 of the rule-family rows in `docs/rule-id-index.md` cited a section that does not define the family (drift from the v0.1 restructure): `CF` (§7.2 → §B.1 / §B.2 / §6.3.4), `CH` (§8.3.3 → §8.3.1), `GOV` (§11.2.6 → §11.1.1 / §7.4.4), `HTLC` (§9.5.3 → §9.5.4), `MA` (§8.5.2 "Multi-claim agreement validation" → §6.3.3 "Bundle-requirement matching", hook §14.3 → §14.1), `PA` (§11.2.6 → §7.4.4), `PIPE` (§8.8 → §9.9, hook §14.3 → §14.4), `PSP` (§8.5.1 "Price snapshot phase" → §7.4.1 "ParserSpec parse/match semantics", hook §14.3 → §14.2), `RA` (§7.4.5 → §7.4.3, plus a copy-pasted description shared with `RAV`), `RD` (§9.6 → §9.4.3), `SIG` (§7.7 → §B.7), `ST` (§10.4 → §10.3.1). The companion §7.9 conformance-summary table had the same `PSP` mis-reference (§7.3.6 → §7.4.1). New `scripts/validate_rule_id_index.py` (wired into CI) ranks each family's defining section by how many of its distinct rule numbers a section carries, and asserts the cited section is the maximal one — so a pointer to a section that merely *references* a family (the failure mode that made the first pass mis-cite `PIPE` and miss `PA`) is rejected; the §14 hook is also checked to resolve. The index is non-normative, so this is a navigation fix, not a rule change. Surfaced as finding D8 in the post-restructure PATH-OS review (#133, cX3po).
- **§9.13 no longer recommends an escrow pattern §9.9 declares inexpressible.** The "Delivery non-delivery" security-considerations note told listings to use "escrow pipelines (`pay-cross-chain-htlc`) where the seller's payment is contingent on demonstrable delivery" — but §9.9 (the #8 fix) establishes that `pay-cross-chain-htlc` is an atomic swap with no mid-lock suspension point, so delivery-gated escrow (lock → deliver → release) is **not expressible in v0.1**. The note now points to the v0.1-expressible orderings (deliver-then-pay / pay-then-deliver, §9.9) and records delivery-gated escrow as roadmapped to the ERC-8183 rail. Surfaced as finding D6 in the post-restructure PATH-OS review (#133, cX3po).
- **§10.11 orchestrator-misclassification mitigation no longer claims an unreachable "source of truth" bundle.** It said an honest party's "independent bundle (with their own classification) is the source of truth for their reputation" — but that bundle is a single-signed `failed-*`, which §10.4.1 and §10.5.1 guard (i) mandate dropping. The mitigation now states the actual mechanism: refusing to co-sign terminates the session `aborted-by-other` (a validly single-signed abort), denying the misclassified bundle its second signature; a party cannot unilaterally publish a competing fault classification, and adjudicating the correct classification is a DACS-X concern. Surfaced as finding D3 in the post-restructure PATH-OS review (#133, cX3po).
- **§10.4.1 no longer references a non-existent `bundleHash.value` field on the AttestationBundle.** The dedup pass (`dc8fe6c`) described the bundle's own hash as a stored `bundleHash.value` field, but the `AttestationBundle` schema has no such field — its hash is *computed* (sha256 of the canonical form, which omits `signatures`/`anchoredByRole`), like every sibling artifact. An implementer reading the prose would look for a field that isn't there, or add a non-conformant one. The signing operand is now named `attestation_bundle_hash` and described as a computed value, not a field — chosen over `bundle_hash` because that token is already DACS-1's IdentityBundle-presentation hash (§6.3.2). No schema, fixture, or vector change: all fixtures already carry only the correct `BundleParty.bundleHash`, and the reference implementation already computes the hash locally. Surfaced as finding D7 in the post-restructure PATH-OS review (#133, cX3po).
- **`observedTransactionalVolume` no longer counts non-completed sessions** (§10.5.1) — the volume walk previously summed `agreement.terms.price` for every reconciled bundle with an `agreementRef`, including failed and aborted post-commit sessions where the agreed value never settled, overstating "transacted" volume (an agent could show volume dominated by sessions that never paid out). The walk now requires `outcome == "completed"`: the metric reports value transacted, not value agreed. Existing golden vectors are unaffected (all current volume expectations are empty). Surfaced as a Low finding in the PATH-OS baseline review (#118, cX3po).
- **§10.4.3(b) one-sided bundle lookup no longer misclassifies a missing anchor as an abort.** The consumer rule previously said "exactly one bundle present → classify the missing side `aborted-by-self`", but the §10.11 bundle-suppression mechanism it cites keys on *signatures*, not anchor presence — so a fully co-signed `completed` bundle whose counterparty copy simply failed to anchor would have read as that party's abort. Rule (b) now classifies by the present copy's signature set: all required signatures → unified session bundle, no abort attributed (a missing anchor is an anchoring omission); single-signed abort outcome → §10.11 suppression classification (unchanged — the `verify-consume-one-sided` golden vector is this case and is unaffected); single-signed any-other-outcome → rejected per §10.4.1, no valid bundle. Aligns §10.4.3(b) with §10.4.1's signature-rejection rule and §10.5.1's reconciliation guard (i). Surfaced as a Med finding in the PATH-OS baseline review (#118, cX3po).
- **Reputation-risk prototype fixture aligned to DACS-5 terminology.** The advisory `suspiciousPatternFlags` fixture now wraps a `reputationDerivation` rather than a stale `reputationRecord`, records `windowingBasis`, and keeps the field clearly non-normative / roadmap-only. The mixed-bundle reputation fixture also records the windowing basis used for §10.5.3 re-derivation.
- **Demos logical→native address mapping did not match the StorageProgram SDK** (§6.2, §6.3.4). v0.1 stated `native_address := "stor-" + sha256(logical_address)` and required consumers to recompute the native address from the logical pattern. The actual Demos SDK (`storage/StorageProgram.ts`) derives `"stor-" + first40hex(sha256(deployerAddress + ":" + storageProgramName + ":" + nonce + ":" + salt))` — folding in the deployer address and the per-write transaction nonce (truncated to 40 hex), so the native address is **not** recomputable from the logical address alone. The §6.2 universal rule now distinguishes **pure mappings** (consumer recomputes directly) from **write-input mappings** (implementation MUST publish the logical→native binding via record metadata + discovery; consumer resolves through it); the §6.3.4 Demos binding shows the real derivation and requires binding publication. The CF-4 delimiter-encoding (logical-address reversibility) and this correction coexist: CF-4 governs the *logical* address; the *native* derivation is the write-input case. Surfaced by the `agent-commerce-demo` reference implementation (#106).
- **HTLC cross-chain settlement actor topology was internally contradictory** (§9.5.4, HTLC-7, HTLC-9, §9.8). The procedure, the HTLC-7 safety proof, and the HTLC-9 asymmetric-state evidence named conflicting actors for who claims/receives on which chain, and the claim order depended on an undefined `flow` selector. Corrected to the canonical atomic-swap topology (verified against Tier Nolan / bip-atom, the Bitcoin Wiki, COMIT, and Lightning): the **payer** (preimage holder) locks the **longer-timelock source** (beneficiary = payee, refund → payer); the **payee** locks the **shorter-timelock destination** (beneficiary = payer, refund → payee); the **payer claims the destination first** (revealing the preimage), then the **payee claims the source**. `flow` deleted. HTLC-9 corrected (the **payer** — not the payee — receives on the destination in the asymmetric state; the marker `dest-revealed-source-unclaimed` and the never-refund/`correction` rule are unchanged, so the DACS-X correction flow is unaffected). Added **HTLC-10** (free-option disclosure) and a conservative finality-margin requirement (≥ 2× destination P99 latency), and tightened HTLC-5's input-uniqueness/salt-loss conditions. Two reference-impl conformance items tracked (reveal order; HKDF derivation) — app-layer only, no contract or SDK change. (#24)
- **Settlement/delivery inconsistencies fixed** (§8.5.2, §9.4.5, §9.5.5, §9.6.2/§9.6.3): (i) delivery handlers read entitlement/attested-payload parameters from the **DeliverableSpec** (the listing's `offering.deliverable`, hash-bound per §8.5.2), not from the `DeliverableRef` in `agreement.terms.deliverable` which carries none of them; `verificationMethod` added to the attested-payload variant (#77); (ii) EntitlementRecord renewal gets a `renewalSeq` address discriminator (`dacs4:entitlement:{jobId}:{renewalSeq}`) so a renewal no longer collides with the original on deterministic storage (#83); (iii) the §8.5.2 settle-by deadline is bounded against `committedAt` (the SR-2 commitment anchor timestamp, same clock as SE-2) not the self-reported `generatedAt` (#90); (iv) §8.5.2 now re-checks `validity.notAfter ≥ committedAt`, closing the read-to-commit expiry interval (#86); (v) §9.5.5 forbids silent rail-mechanism substitution (no tank→HTLC fallthrough; `txRef.kind` MUST match the pinned rail), with flow-trace.md reconciled (#60); (vi) RAV-R4 narrowed to detect rail failure at the settlement attempt (availability is pinned at session start, so a mid-session flip isn't observable) — a proactive liveness probe is roadmapped (#23). Closes #23, #60, #77, #83, #86, #90.
- **x402 evidence is now chain-verifiable + rail-protocol-version captured** (§9.5.6, §9.5.7, §9.3 txRefs, §12.5). x402 settles on-chain (a gasless USDC transfer on its settlement chain, e.g. Base) and the facilitator returns the on-chain tx hash in the `PAYMENT-RESPONSE` header — but the evidence carried only the receipt hash, so §9.13/§12.5's "verifiers MUST validate" was unsatisfiable from the anchored artifact. The x402 txRef now carries `settlementTxHash` + `chainId`, making the record **chain-verifiable directly against the settlement chain, like the `evm` rail** (the primary audit path; the receipt hash is supplementary, with a documented fallback for facilitators that don't return a tx hash) (#28). Both the x402 and AP2 txRefs now carry a required `protocolVersion`, so historical evidence is re-validatable against the wire rules of its era (#27). Closes #27, #28.
- **EntitlementRecord gained `serviceEndpoint`** (§9.6.2) — a `deliver-entitlement` grant previously named no callable endpoint, so a buyer holding a valid entitlement had nowhere to present it. The record now carries `serviceEndpoint` (where the grantee exercises the grant), making it a self-contained receipt — grantee, scope, validity window, and access location in one signed artifact. Credential/token handover (how to authenticate, vs. presenting the record as bearer proof) is left to a roadmap item. Closes #61.
- **Security floors made enforceable** (§7.3.2, §7.3.5, §6.3.2, §8.12, §9.4.5, §12.4) — six places where the spec *claimed* a defence that, as written, didn't hold (highest-severity; pre-launch v0.1 tightenings): (i) **VP holder-binding** — `verifiable-credential` now requires the presenter to prove control of the credential subject, bound to the session nonce, so a verified VC can't be replayed by a non-holder (#1); (ii) **negative-match completeness floor (PSP-5)** — `ofac-clear` and any full-list negative-match MUST confirm the response is complete (record-count/sentinel/Content-Length) before returning "clear", closing the truncated-SDN fail-open that would clear a sanctioned party (#5); (iii) **SIWD cross-chain binding clarified** — SIWD validates against the Demos wallet's key with the primary claim (any chain) bound via the wallet's CCI/SR-1 link, not by requiring the primary claim to be EVM (#16); (iv) **channelId uniqueness (CH-6)** — required per session so the cross-session offer-replay defence actually holds (#9); (v) **availability-field poisoning (RAV-R5)** — availability MUST be read from the authoritative signed/anchored rail definition, not a poisonable cache (#48); (vi) HTLC preimage-reveal front-running added to the §12.4 threat catalogue (beneficiary-bound claims prevent theft) (#52). The §7.12 SR-3 mitigation is corrected to honest "alternative independent methods" — the undefined `requireAll` single-claim multi-method-AND claim is removed (cross-claim AND is already enforced by `BundleRequirement.required`; single-claim multi-method-AND is roadmapped) (#4). §12.4 catalogue + §14 conformance updated. Closes #1, #4, #5, #9, #16, #48, #52.
- **Threat-model, conformance, and versioning hygiene** (§11.1.2, §11.2.5, §6.3.4, §10.11, §12.2, §11.1.1, §7.4.4, §14): (i) the registry-index address (`dacs2:registry:v0.1` / `dacs4:registry:v0.1`) is clarified as the **major-version line** — minor updates append to the same index additively; "frozen at v0.1" means baseline entries are immutable, not that the index stops growing (#76); (ii) `*Version` literals are clarified as **major-only**; the listing "dacsVersion supported" gate is a major check in v0.1; per-artifact minor-version signalling and the older-reader-newer-minor decode direction are roadmapped (#79, #94); (iii) the §10.11 reputation-volume anti-collusion claim is softened to match its mechanics — per-currency, unnormalised, evadable across currencies; consumers must not over-trust it (a transaction-count + FX-normalised metric is roadmapped) (#31); (iv) §12.2 gains the missing adversary classes (malicious infrastructure, malicious verifier, spam/resource-exhaustion) and a note reconciling the §12.4 column — variant labels map to existing classes, and "time"/"implementation bug" are environmental/robustness conditions, not adversaries (#40); (v) governance MUSTs are labelled **GOV-1..GOV-3** with a §14.7 conformance category, and GOV-3 is shown satisfiable from the recipe's existing `governance.anchoring` field (#49). Closes #31, #40, #49, #76, #79, #94.
- **Anchored-data confidentiality posture made explicit** (§12.1, §12.2, §12.4, §8.2, §7.5) — the audit layer anchors party primary claims (durable authority IDs) in cleartext at derivable addresses, so a passive observer can reconstruct the counterparty graph and read each vet attestation's `scheme:identifier:decision` with no cryptography. v0.1 **accepts this by design** (accountability over privacy) and now *says so*: the §12.1 non-goal scope is extended from SR-4 channel membership to the DACS-5/commit/vet audit layer, the §12.2 Network-observer capability includes graph reconstruction, §12.4 gains explicit rows, and §8.2's "little privacy cost" framing is softened (#68, #78). One real bug fixed alongside: a new **§7.5 public-anchor data-minimisation MUST** — a publicly-anchored `VerifyResult.data` carries only predicate outcomes, never raw extracted PII/financials, so a `tlsnotary`/`zktls` recipe (which exists to prove a fact without revealing the value) can't have that value copied into a cleartext anchor (#78). Encrypted-to-parties anchoring + hashed addresses (the full confidentiality layer) is roadmapped. Closes #68, #78.
- **Cross-section contradictions resolved** (§7.8.2, §9.9, §14.5, §14.9): (i) a required claim returning `decision=fail` is no longer double-mapped — the §7.8.2 table is reconciled to VPC-4 (conclusive `fail` → `counterparty`; `permanent` reserved for malformed bundles / retry-exhausted `error`-`indeterminate`), closing a reputation-bucket steering surface (#13); (ii) **PIPE-1 relaxed** to require at least one *deliver-* phase with pay-* optional, so **intake-only / out-of-band-settlement listings** (which §6.3.4(8) explicitly blesses) are no longer rejected by the pipeline executor (#43); (iii) the §14.5 "divergent-bundles" test now asserts the observable §10.4.3(d) per-party policy rather than a `disputed` value the `outcome` enum doesn't carry, and §14.9 clarifies divergence *detection* is in-scope for v0.1 while dispute *resolution* (DACS-X) is deferred (#65). Closes #13, #43, #65.
- **Contributor-PR reconciliation (cX3po, #35/#89/#29/#66).** Four cX3po PRs that the evaluator-gated review rounds had independently resolved on `main` were reconciled by adopting the contributor's wording where it was the better expression, then closing the PRs with credit: **#35** — the SIWD presentation-binding `Resource` is now `dacs:<hex>` (lowercase hex of the full signed_bytes) rather than the `urn:dacs:bundle-presentation:v1:<hash>` form, matching `docs/flow-trace.md` (the reference implementation) exactly (§6.3.2, §14). **#89** — the negotiable-band bounds get an explicit rounding mode: each bound rounded half-up to `bandCenter.amount`'s CD-1 fractional-digit count, with "reject if computed lower bound ≤ 0" (§8.5.2). **#29** — `ReputationDerivation` gains a determinism receipt: new `windowingBasis` field, `bundleRefs` MUST be exactly the §10.5.1 `reconciled` set in canonical ascending-`contentHash` order, and re-derivation under the recorded basis MUST reproduce byte-identical metrics + bundleCount (new §10.5.3 normative paragraph, §10.8, §14.5). **#66** — new **PC-7** cross-chain anchor/settlement decoupling: once foreign-chain value movement is irreversibly confirmed, the handler returns `ok: true` with foreign txRefs even when SR-2 anchoring is pending (idempotent durable anchor-retry, deferred attestationRef), MUST NOT report `errorClass:"substrate"`, and explicitly excludes the HTLC-9/ST-8 asymmetric state; `substrate` is re-scoped to pre-irreversible-movement only (§9.5.1, §9.5.4, §9.5.5, §14.4). Closes #29, #35, #66, #89.
- **Round 9 — front-matter section renumbering (the deferred structural item) + final cleanup.** Resolved the long-deferred dual section-numbering: the front-matter reference sections "6. Demos production mapping", "7. Global terminology", "8. Composed open standards" shadowed Chapters 6/7/8 (DACS-1/2/3), making bare §6.x/§7.x/§8.x citations ambiguous (notably §7.5 front "Shared phase-handler types" vs chapter "VerifyResult", and §7.7 front "Universal signature scheme" vs chapter "Composite verification record"). Lettered the three front-matter sections to §A/§B/§C (§A.1–A.5, §B.1–B.7), leaving Chapters 6–14 and §1–§5 unchanged. Every citation was reclassified by reading context — three-level §N.x.y are all chapters (unchanged); §7.7 signature-scheme refs → §B.7 (keeping the two chapter composite-record refs as §7.7); §7.5 shared-types → §B.5 (keeping chapter VerifyResult/minimisation as §7.5); §7.2 anchoring/CF-1 → §B.2; §6.x/§7.4/§8.x are single-home and unchanged. Added a "Section numbering" note to About-this-document. **Final cleanup pass:** a verification sweep caught two prose "chapter N" leftovers pointing at front-matter content ("chapter 5's SessionContext/PhaseHandlerResult" → §B.5; "composed-standards table in chapter 8 (front matter)" → §C); a whole-document copy-edit sweep (editing artifacts, stale version refs, cross-ref resolution, rule-family count-ranges, terminology, table integrity) came back clean. This closes the defect-hunt loop begun at Round 1.
- **Internal deep-dive review — Round 8 (full sweep, evaluator-gated).** 5-finder sweep → impact-evaluator → six commits → invariant gate. **No Criticals, no Highs** (second round running); DACS-4 returned nothing material. 4 Mediums (three self-inflicted by R7, one pre-existing subtlety) + Lows. **R8-A:** the R7-7 `bundleToVet.sessionNonce` pin didn't cover SIWD-presented bundles (which carry their nonce in the SIWD message `Nonce` field, not the top-level field) — three reviewers flagged it, and the R7 evaluator had warned against the over-narrowing; replaced with the §6.3.2 disjunction in §7.3.2/§7.3.6, and added the missing `maxTokenAgeSec` slot to the oauth-attested recipe variant. **R8-B:** the MA-3 / no-selector reputation rule re-checked the presentedBy claim's decision but not its freshness — folded in the §6.3.2 staleness gate so a passing-but-stale high-tier claim can't key reputation. **R8-C:** added `dacs5:rating:{jobId}:{rater}` to the §6.3.4 CF-4 enumeration + §14.6 round-trip (R7-3 fixed the sibling `dacs2:` addresses but missed this colon-bearing one). **R8-D:** derive()'s empty-input early return now returns a schema-total record (`bundleRefs=[]`, `observedTransactionalVolume=[]`, scalar metrics null) — R7-5 had set `bundleRefs` only on the main path and "all metrics null" was type-incorrect for the non-nullable array metrics. **R8-E:** a counterparty could escape the at-fault reputation class by malforming one `oneOf` alternative (→ error → permanent → local fault); added a fault-attribution carve-out (counterparty-malformed presentation → `counterparty`, observable provenance), strictly attribution-only — the R7-2 OR-semantics decision precedence is untouched. **R8-F:** glossary re-sort. The §6/§7 dual section-numbering remains the one deferred editorial item.
- **Internal deep-dive review — Round 7 (full sweep, evaluator-gated).** 5-finder sweep → impact-evaluator → seven dependency-ordered commits → invariant gate. The cleanest round yet: **no Criticals, no Highs.** ~4 Mediums (two self-inflicted by R6, two pre-existing) + editorial. Notably, the apply step *overturned* one finder+evaluator recommendation on analysis (see R7-2). **R7-1:** aligned VP-C1 + the §14.4 caching bullet to §6.3.2's `??`-fallback freshness window (R6-3 had made VP-C1 a `min`-of-both that falsely claimed equivalence — an authority's explicit validUntil governs; the default is only the fallback); maxAge comment "tightens never widens". **R7-2 (corrected):** the proposed `find_fail`-first fix to the oneOf aggregation would have been a *bug* — a oneOf group is OR-semantics, so a retryable `error` alternative correctly outranks a `fail` (the group isn't conclusively unsatisfiable); fixed the three misleading comments instead and documented the within-group (error>indeterminate>fail) vs cross-accumulator (fail>error>indeterminate) precedences. **R7-3:** extended CF-4 percent-encoding to the `dacs2:` verification + composite addresses (their `{identifier}`/`{evaluatedParty}` carry internal colons — same class as the R6 railId fix). **R7-4:** defined the HTLC-7 `source_finality`/`safety` as pinned rail parameters `sourceFinalitySec`/`safetyWindowSec` (R6-6 left them undefined → non-deterministic route validation), relocated the dest-finality concern off the source-margin onto the payer's dest-claim window, and added the payer dest-claim finality SHOULD. **R7-5:** added a no-scoring-consumer fallback to §10.4.3 case (c) (it referenced the deriver-only "scored party") and populated derive()'s required `bundleRefs` output. **R7-6:** registered `dacs-sealed-bid:v1:` as a commitment-hash domain (scoped out of SIG-1) and added the SIWD-URN bundle-binding conformance test. **R7-7:** removed an R6 duplicate sentence, added two §12.4 residual rows (HTLC asymmetric-loss blame ambiguity; HTLC-10 free-option), re-sorted two glossary entries, pinned the VC/oauth nonce target to `bundleToVet.sessionNonce`. The §6/§7 dual section-numbering remains the one deferred editorial item.
- **Internal deep-dive review — Round 6 (full sweep, evaluator-gated).** 5-finder sweep → impact-evaluator (blast-radius/ordering map) → six dependency-ordered commits → 14-invariant verification gate. The R5 wave audited clean (drift pass found only 2 pointer drifts — the evaluator gate is holding), but the finders surfaced two new Criticals and several deep pre-existing issues the R5 edits exposed. **R6-1:** PricingSpec auction `selectionRule` type tightened from a degenerate `| string` to the exact phase-step enum (so the "MUST equal" rule is type-expressible); declared `railId` a CF-4 variable segment (the R5 `:{phaseIndex}` payment address was unparseable because railIds contain colons); fixed the brittle `line 2726` cite, the `committedAt` §8.5.2→§8.6 cite, the §8.12 front-running text, and added a §12.4 attestation-bundle-replay row. **R6-3:** freshness window pinned to the recipe at `VerifyResult.recipeVersion` (closing an admission-divergence from "latest"-recipe lookup) at all sites, inverted-window fail-closed, VP-C1 reworded OR→min so reuse and freshness agree; §6.3.4→§6.3.2 staleness cite fixed. **R6-4:** the SIWD bundle-binding Resource is now a valid EIP-4361 URI (`urn:dacs:bundle-presentation:v1:<hash>`) rather than a bare hex string. **R6-5:** "canonically diverges" defined once (outcome/phaseSummary contradiction, not advisory-field skew) — closing a consumer-vs-deriver verdict split and a perturbation-grief surface. **R6-6:** `settle-asymmetric` can now pause on an SR-2 outage (ST-7 broadened) instead of wedging a successful late swap into spurious `failed-counterparty`, with the failed-substrate classification scoped so it can't launder a payee loss; and HTLC-7's finality margin re-derived to cover the payee's **source**-chain claim finality (was dest-only — a slow-source/fast-dest route could strand the payee). Deferred: the §6/§7 front-matter-vs-chapter dual section-numbering (incl. the duplicate `### 7.6`) to a dedicated editorial renumbering pass (high cross-ref ripple, no active broken reference).
- **Internal deep-dive review — Round 5 (full sweep + impact-evaluator gate).** A 5-reviewer finder sweep feeding a new **impact-evaluator** stage (map each fix's blast radius, interactions, and new-contradiction risk) before any edit, plus an 18-invariant verification gate after. The evaluator caught two would-be circular fixes: a finder-proposed new state-machine sub-state was downgraded to a prose clarification (the transition table is per-stage, so a sub-state would have broken the closed pair-set), and the fix ordering was corrected (the derive() divergence gate would false-positive unless `anchoredByRole` was de-hashed first). Applied in six dependency-ordered commits. **R5-1 (Critical):** `anchoredByRole` (added R4-B) was in the hashed canonical form, so the two-sided bundle copies always differed → every happy-path session mis-routed to "disputed"; excluded it from the canonical form (like `signatures`) and restored its integrity via an anchor-address cross-check. **R5-2:** derive() reconciliation now validates §10.4.1 signatures first (a single-signed non-abort copy was being perspective-flipped to tank a victim's score), excludes genuinely-divergent jobIds from both numerator and denominator, and scopes to buyer/seller copies (orchestrator copies evidence-only). **R5-3:** the ST-8 settlement cluster — `supersedesEvidenceRef` added so the `:resolved` record can reference the interim one; §10.4.3 carve-out so both copies list one identical entry; PC-2 payment address gains a `:{phaseIndex}` discriminator (repeated pay-* phases under PIPE-5 collided); finality gates on entering/resolving the asymmetric state; per-stage-resume prose (no new sub-state). **R5-4:** bundle freshness now keys on the resolved VerifyResult's verifiedAt/validUntil (presenter wrapper timestamps clamped) at both sites; SIWD Resource-line verifier check added. **R5-5:** sealed-envelope selection operates over the anchored reveal set (relay-suppression resistance), currency/positivity exclusion before reserve, reserve semantics+CD-1+inclusivity defined for all selection rules, generalized empty-set termination, bidHash lowercase-hex for deterministic tie-break. **R5-6:** disclosed the unbounded negotiable `maxPct` upside.
- **Internal deep-dive review — Round 4 (steward-side defect hunt).** A 5-reviewer parallel pass (per-stage chapters + a whole-document drift pass seeded with the Round-3 diff). Two reviewers independently found a cross-chapter soundness contradiction; several findings were again self-inflicted by the prior fix waves. **Critical:** (R4-A) the HTLC-9 asymmetric-settlement `correction` resolution was **unreachable** — the phase returned a terminal `settle-failed`/`failed-counterparty` ("never reopened"), so a swap where both parties actually got paid was permanently recorded as a buyer-blamed failure, and AMEND-2 called the htlc-claim-carrying `correction` "non-financial". Fixed (per steward decision) by adding a real non-terminal `settle-asymmetric` state + rule ST-8: the session waits until `expiry_source`; an `htlc-claim` resolves forward to `completed`, window-expiry to terminal `failed-counterparty`; no `correction` amendment is used, so the success path produces a normal `completed` bundle and the AMEND-2 contradiction is gone. (R4-B) `derive()` double-counted aborts across two-sided bundles — the `scoped` set admitted both anchored copies of a jobId and the partitions read raw `outcome`, so a grief victim picked up the withdrawer's `aborted-by-self` copy (inverting the §10.11 guarantee); the prose even falsely claimed `scoped` restricted to self-anchored bundles. Fixed by adding `anchoredByRole` to AttestationBundle and a per-jobId reconciliation that interprets `outcome` from the *scored* party's perspective (literal for the self-anchored copy; `perspective_flip` for the counterparty-only copy), with all downstream metrics on the deduped set. **High (R4-C):** defined the negotiable price-band math (was referenced but never specified) + fixed-price-over-negotiable equals-center + authoritative selectionRule; wired the §6.3.4 staleness rule into `find_claim` unconditionally; barred keying reputation on an unverified `presentedBy` (laundering hole when no selector); made the revocation marker non-replayable (domain-separated payload + listingId/version/contentHash binding); corrected the HTLC finality-model decisive-event wording; enforced auction `reservePrice`, made bidders self-anchor reveals, and added an orchestrator/seller reveal-manipulation threat entry; reclassified the recoverable tank-lock as `substrate` (reputation-neutral) not `failed-counterparty`; fixed the §14 stale HTLC-2 test. **Medium (R4-D):** added the `sessionNonce` bundle field + reconciled presentedAt/SIWD nonce binding; added oauth-attested session-binding; made `paymentAmount` required on success; fixed §14 `commitmentLevel` naming + widened the RAV test range to RAV-1..7 + added §7.9 RAV rows; marked the sealed-envelope `commitTimestamp` informational-only. ROADMAP: added a sealed-envelope simultaneous-reveal candidate.
- **Internal deep-dive review — Round 3 (steward-side defect hunt).** A 5-reviewer parallel pass (one per DACS stage chapter + a whole-document integration-drift pass); every Critical and the drift fixes were re-verified against the file before fixing. **Critical (R3-A):** (i) §6.3.3 freshness off-by-1000× — `issuedAt` is unix ms but `maxAge`/`defaultMaxAgeSec` are seconds and the staleness comparison did no conversion (a 60s OFAC window actually permitted ~16.7h), plus an absent `issuedAt`/`expiresAt` now fails closed instead of passing via a NaN compare; (ii) §9.5.4 HTLC canonical-success evidence was unrepresentable — added the `htlc-claim` `ChainTxRef` kind (the payee's source-side claim) the R2-D prose already required but no union member could carry, and updated the §9.14 cell; (iii)+(iv) §10.5.1 `derive()` had two non-executable expressions (`party in b.parties.primaryClaim`; `|failed_counterparty against party|`) corrected to the idioms already used in-algorithm. **High (R3-B):** VPC-4 `indeterminate`/`error`→"transient or permanent" contradiction with VP-R4 removed (terminal ⇒ permanent); `ListingTerms.acceptanceModel` added so §8.4.1 auto-accept is declarable; §8.5.2 non-existent `negotiable.minPct/maxPct` path corrected to the variant fields; HTLC-2 salt-disclosure rule rewritten (the salt is never revealed on-chain, so gating on source-lock finality was wrong); `BundleSignature.value` comment reconciled with the §10.4.1 domain-separated payload; dead `failed_perm`/`aborted_by_self` bindings annotated; ST-7 abort-vs-substrate-pause precedence added. **Medium/Low (R3-C):** §5 `CH-1..CH-5`→`CH-1..CH-6`; `offerings.category`→`offering.category`; `recipe.parser`→`recipe.parserRules`; `finalityCommitmentLevel`→`commitmentLevel` rail-param comment; CH-6 promoted into the §8.3.1 required-properties list; §8.4.3 now collects the winning bidder's co-signature (§8.5.1); auto-accept `committedAt` circularity fixed per the §8.5.2 ordering note; VPC-2 reworded to once-per-party; PC-4 settlement-atomicity gloss broadened to the HTLC-9 case; AMEND-3 same-currency-refund rule; §10.5.1 rating de-duplication rule + completionRate-denominator/griefing note; the lone ambiguous `§6.4` cite disambiguated. Two genuine design additions were routed to ROADMAP rather than patched into v0.1: session-bound settlement evidence on single-chain rails (the rail-replay gap) and a blame-weighted completion metric; plus cross-currency refunds and pay-solana-spl ATA-rent handling.
- **Internal deep-dive review — Round 2 (steward-side defect hunt).** A 6-reviewer parallel pass over the whole spec, focused on residual defects + drift introduced by the 48h fix wave. Fixed ~28 findings across four batches: **(R2-A, self-inflicted)** §10.5.4 category-scoped derivation referenced non-existent `AgreementDocument.offering.category` (PR #111) → rewritten to the listingRef→Listing resolution; §8.5.2/§8.6 `committedAt` checks were ordered before `committedAt` exists (our #90) → ordering note added; the §7.4.1 `lei` example violated the §7.5 public-anchor minimisation MUST (our #78) → MUST scoped to private fields with a public-registry carve-out; §9.10 PC-1..PC-5→PC-6 (PR #109); §8.9/§5/§8.11 CH-1..CH-5→CH-6 (our #9). **(R2-B, pre-existing)** §6.3.3 match() read `.decision` off `VerifyResultRef` (no such field) → read from the resolved VerifyResult, and added the MA-1..MA-3 labels §14.2 cited; §7.3.2 VC parse-failure mapped to `indeterminate`→`error` (retryable); RAV-3 now forces `mocked` recipes to `error` (closed a stub fail-open). **(R2-C, cross-ref/numbering)** §12.4 PSP-5 cite §7.3.5→§7.4.1; §9.4 numbering gap closed (9.4.5→9.4.4); RailDefinition `registryVersion`→`railVersion`; §9.13 decimal-overflow cite PC-1→§9.5.2; settlementFinality "optional"→required-on-success; x402 finality model reconciled with #28; dead `party_fault_count` removed; `completionRate` null-condition comment corrected; `substrate-validator-set` registered as a scheme; auto-accept now checks `committedAt ≤ validUntil`. **(R2-D, editorial/hardening)** signature-payload wording (BP-4/sr1-root/ListingSignature), `preferredPresentation` gains `siwd`, idempotency carve-out broadened, aggregation helper definitions, HTLC `success` now requires the payee source-claim txRef, rating fetch/verify exclusion, PS-1..PS-3 conformance bullet, §9.14 card ap2/x402 notes, ST-7 resume clarification, RFQ-body + first-acceptable-predicate determinism notes.

---

## [0.1] — 2026-05-31

First publicly released version.

Earlier drafts circulated internally under per-stage version numbers (DACS-1..5 v0.1) attached to a paper version (v0.7), with a brief v0.8 cut that consolidated review-pass revisions. Those numbers are retired and reset to a common baseline. v0.1 publishes all five per-stage standards together; from this baseline onward each per-stage standard versions independently.

External implementers who saw earlier drafts (most notably PATH-OS Labs' `pathos-dacs-ref`, which was built against the v0.7 paper / v0.1 per-stage state) should treat this entry as the migration list for what changed between their drafting target and v0.1.

### Added

- **`sr1-root` variant on `PresentationSignature`** (§6.3.2). New variant: `{ kind: "sr1-root"; rootClaim: ClaimReference; aggregateSignature: string }`. The natural presentation kind for a party self-binding a single document — a seller signing their own listing, an orchestrator binding multiple per-substrate addresses under one identity. Previously, the spec referenced `sr1-root` in §6.3.3's `preferredPresentation` enum and §6.3.4's listing-signature rule, but the union didn't define it; implementers fell back to `per-claim` with synthetic key claims. The domain-separated payload (`"dacs-bundle-presentation:v1:" || bundle_hash`) is shared with the other three presentation kinds.

- **`VerifyResult.decision` fourth value: `error`** (§7.5.1). Decision is now `pass | fail | indeterminate | error`. Distinction:
  - `indeterminate` — authority responded, response parseable but conclusively neither confirms nor contradicts the claim. The authority's indeterminate answer.
  - `error` — verification could not complete (transport failure, SR-3 fetch timeout, parser exception). The verifier never received an authority decision.
  - Retry rules now distinguish: `error` is the default-retryable value; `indeterminate` is not retried unless `recipe.retryOnIndeterminate: true`.
  - Aggregation precedence: failures > errors > indeterminates. The composite record's `overallDecision` is now also four-valued. New rules VP-R1 through VP-R4 cover the retry semantics.

- **`HTLC-6` normative rule on per-chain hashlock computation** (§9.5.4). Hashlock is the chain-native hash function applied to the preimage. Source and destination chains MAY use different hash functions (keccak256 on EVM, sha256 on Solana/Bitcoin family, blake2b on Cosmos family). The preimage is the only cross-chain-shared value; each side's hashlock binds against its own native hash of that preimage. The previous HTLC-5 rule conflated preimage derivation (one operation, sha256 globally appropriate) with hashlock computation (one per chain, must be chain-native).

- **Logical-vs-native address split, applied universally** (§6.2, §6.3.4). Throughout DACS, addresses of the form `dacs1:…` through `dacs5:…` are *logical* addresses: substrate-independent, human-readable, stable identifiers the protocol reasons about. Each substrate maps them to its native addressing under a deterministic rule. On Demos, the mapping is `native_address := "stor-" + sha256(logical_address)` because Demos's StorageProgram naming does not permit colons. Other substrates substitute their own mapping; the requirement is determinism, one-to-one, and reversibility by any party knowing the logical pattern.

- **`recipe.availability` normative field** (§7.4.5). Every `Recipe` now declares an operational status from a closed enum: `live | operator_gated | closed_data | bilateral | mocked | disabled | failed`. Verifiers MUST inspect availability before running a recipe (RAV-1 through RAV-7). Aggregation under §7.7.1 treats `disabled` and `failed` recipes as decision = `error` regardless of any output. The previous informative legend in §6.1 (🟢/🟡/🔵) flattened distinct operational states into one and pushed disambiguation onto every implementer's UI surface; promoting availability to a normative field moves the disambiguation into the protocol layer where verifiers can reason about it programmatically.

- **`rail.availability` normative field** (§9.4.5). Same value set and semantics as recipe availability, applied to `RailDefinition`. Orchestrator obligations (RAV-R1 through RAV-R4) parallel verifier obligations on recipes. A rail transitioning from `live` to `failed` mid-session maps to errorClass = `substrate`, not `counterparty`.

- **Progressive anchoring phases on registries** (§7.4.4 recipes, §9.4.3 rails). Three explicit phases: PA-1 (bootstrap; in-code constants), PA-2 (single-steward; signed by the steward), PA-3 (constituted; signed by a multi-party governance body). Implementations MUST disclose which phase they operate in; consumers MUST verify the phase against their own trust requirements. The current operating phase for v0.1 is PA-2 with KyneSys Labs as the steward.

- **§11.2.6 Multi-party governance** as named follow-on work. Acknowledges that the PA-2 → PA-3 transition is itself open work that v0.1 does not specify (constitution mechanism, multi-signature thresholds, sub-authority delegation by domain).

- **Glossary entries** for `sr1-root presentation`, `Error (decision value)`, `Indeterminate (decision value)`, `Recipe availability`, `Rail availability`.

- **Conformance test plan entries** for the four-value `VerifyResult.decision`, HTLC-1..HTLC-6 per-chain hashlock testing, RAV-1..RAV-7 recipe availability obligations, RAV-R1..RAV-R4 rail availability obligations.

- **Acknowledgement of `pathos-dacs-ref`** (§11.3 closing). The previous draft listed "second independent reference implementation against the spec" as remaining work. The PATH-OS Labs reference implementation, built independently against earlier drafts, closes this gap. The cross-substrate test of the artifact-level portability claim (an implementation against a non-Demos substrate) remains future work, but the third-party-implementability claim is now empirically tested.

### Changed

- **DACS-5 bundle separator renamed** from `dacs5-bundle:v1:` to `dacs-bundle:v1:` (§7.7, §10.4.1). The earlier name was the lone outlier in the universal separator scheme (`dacs-<kind>:v<ver>:`); a universal separator builder could not produce it. Bundles signed under the old separator will not verify under the new one (which is the point — domain separators exist to prevent cross-context reuse). The "preserved verbatim for backwards compatibility" justification in earlier drafts was never valid as no reference implementation had shipped under the old name.

- **`Listing.acceptedRails` is now optional** with a conditional rule (§6.3.4). Previously declared `acceptedRails: PaymentRailRef[] // non-empty`, which forced sentinel `railId: "none"` workarounds for intake-only listings. The schema is now:
  ```
  acceptedRails?: PaymentRailRef[]      // required iff pipeline contains any pay-* phase
  ```
  Intake-only patterns explicitly accommodated in validation step 8: RFP intake, reverse auctions where the bid is the commitment, free services gated by reputation, sealed-bid procurements settled out-of-band.

- **HTLC-5 narrowed** (§9.5.4). Previously a single rule covering both preimage derivation and hashlock computation. Now scoped to preimage derivation only: HKDF per RFC 5869, sha256 KDF, IKM = buyerSalt, salt = jobId, info = agreementHash. Hashlock computation moved to HTLC-6 (see Added).

- **`overallDecision` type on `CompositeVerificationRecord`** expanded to four values (§7.7). Matches the four-value `VerifyResult.decision`. Failure-reason text distinguishes "required errored" from "required indeterminate" from "required failing" so consumers can debug.

- **Aggregation algorithm** rewritten to handle four classifications with precedence `failures > errors > indeterminates` (§7.7.1). The `classify_required` helper now distinguishes all four cases; `oneOf` group resolution similarly distinguishes "at least one indeterminate" from "at least one errored" from "no claim satisfied at all".

- **§6.1 production-mapping legend** (🟢/🟡/🔵) clarified as informative-about-substrate-features only. Per-recipe and per-rail operational status uses the normative `availability` fields in §7.4.5 and §9.4.5 — the legend no longer carries the disambiguation it used to.

- **§11.1 Governance and versioning** renamed to **§11.1 Stewardship and versioning** and rewritten throughout. The previous §11.1.1 "The proposal" was sales-shaped (positioning Demos as the substrate, listing what needed to happen with composed standards); replaced by **§11.1.1 Current steward**, which names KyneSys Labs honestly and discloses the single-steward arrangement.

- **§11.3 closing** rewritten. The previous version led with "the closed-marketplace alternative is consolidating fast" and positioned DACS as Demos's contribution to keeping the lifecycle open. The v0.1 closing is descriptive rather than positional, acknowledges the PATH-OS reference implementation explicitly, and lists honest remaining work without sales register.

- **Versioning scheme.** Earlier drafts used dual versioning (paper v0.X + per-stage v0.Y) with diverging per-stage numbers. v0.1 retires those and resets all five per-stage standards to a common baseline, published together. From this baseline onward each per-stage standard versions independently; a pipeline composes a coherent set of per-stage versions.

### Removed

- **All "working-group multi-sig" language from normative requirements.** Affected: RA-1 (recipe signature), RD-1 (rail signature), recipe signature type, rail signature type, recipe-poisoning threat mitigation, validator-set claim text, closed-method rationale, closed-rail-registry rationale, trust-boundary threat-model text. No working group is constituted. References that previously asserted multi-sig governance now describe the steward's signing key, with multi-party governance named as follow-on work in §11.2.6.

- **§7.4.4 recipe sub-authority delegation.** Earlier drafts described sub-authority delegation by domain (sanctions-list sub-authority, financial-regulation sub-authority). No such sub-authorities exist; the structure that would constitute them does not exist. The section now describes the steward's lifecycle responsibilities and the progressive-anchoring path; sub-authority delegation is open work for v0.2+.

- **The §11.1.1 "Demos proposal" sales-pitch material.** What positioned Demos as the substrate and listed actions to be taken with composed-standards maintainers. Substrate and composition stances retained as factual statements in §11.1.4 and §11.1.5; the proposal framing removed.

- **`dacs-bundle:v2:` forward reference.** Premature v2 mention in the v1 spec. Removed.

- **The previous "preserved verbatim for backwards compatibility" note** on the bundle separator. The compatibility concern it cited never materialised (no reference implementation had shipped under the old name).

### Fixed

- **`sr1-root` was referenced but undefined.** Spec referenced the value in §6.3.3's `preferredPresentation` enum and §6.3.4's signature rule but did not define it as a variant of the `PresentationSignature` union. Now defined (see Added).

- **`dacs5-bundle:v1:` broke the universal separator pattern.** The §7.7 registry promised `dacs-<kind>:v<ver>:` for every separator but the DACS-5 bundle separator was an outlier. Renamed to fix (see Changed).

- **`acceptedRails: PaymentRailRef[] // non-empty` was incompatible with intake-only listings.** Sealed-bid government procurements, RFPs, reverse auctions, free services — none of these have a per-listing settlement rail, but the schema required one. Fixed by making the field conditionally optional (see Changed).

- **The `dacs1:…` logical anchor address was not realisable on Demos.** §6.3.4 said "the recommended scheme on Demos uses StorageProgram with address `dacs1:{primaryClaim}:{listingId}:v{n}`," but Demos rejects colons in StorageProgram names. The addressable handle is the sha256-derived `stor-<hex>`. Implementations were correctly using a colon-free native address and carrying the `dacs1:…` string as descriptive metadata; the spec now matches that behaviour. The same logical/native split now applies across all DACS address patterns universally (see Added).

### Governance

- **KyneSys Labs is named as the v0.1 steward** (§7.4.4, §9.4.3, §11.1.1, threat-model trust-boundary text). Single-signer arrangement; progressive-anchoring phase PA-2.
- **PA-2 residual risks acknowledged.** Steward-key compromise is the principal residual risk under PA-2 single-signer governance; multi-signature governance under PA-3 is the v0.2+ mitigation pathway, conditional on the constitution of a multi-party body.
- **PA-3 transition is open work.** §11.2.6 names this explicitly. v0.1 does not specify the constitution mechanism, multi-signature thresholds, sub-authority delegation, or transition procedure.

---

## Earlier drafts (retired)

The following internal numbering is retired and replaced by the v0.1 document-level versioning above:

- **Paper v0.7 + per-stage DACS-1..5 v0.1** — the longest-circulating internal draft. Reviewed by PATH-OS Labs (whose reference implementation `pathos-dacs-ref` was built against this state).
- **Paper v0.8 + per-stage DACS-1..5 v0.2** — a brief consolidation cut that incorporated review-pass revisions (the four-value decision, the universal separator rename, the conditional `acceptedRails`, the logical/native address split, the steward framing). Not externally circulated under this version number.

If you implemented against the v0.7 / v0.1 state and are migrating to v0.1, the **Added** and **Changed** sections above are your migration list.

---

## Format conventions

- Versions follow `v0.MINOR` style during draft phase (v0.1, v0.2, …) and `vMAJOR.MINOR` once a standard reaches v1.0. Each per-stage standard versions independently from the shared v0.1 baseline.
- Breaking changes get a major-version bump (v0.x → v1.0, v1.x → v2.0).
- Additive non-breaking changes get a minor-version bump (v0.1 → v0.2).
- Editorial-only changes (typo fixes, formatting, examples that don't change semantics) do not bump the version.
- Each release entry cites section numbers for every change so implementers can scan against their own code.
